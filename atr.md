# ATR exit（`side=+1` 多、`-1` 空；`atr=ta.atr(len)`）

1. `stop=close-side*atr*slM; limit=close+side*atr*tpM; strategy.exit("X",from_entry="E",stop=stop,limit=limit)` — `Gold_Intraday_EMABBVWAP_ATR_SLTP_SessionWeekday_Filter.md:74-77,108-112`、`Ichimoku_Confluence_Strategy.md:216-234`、`HyperliquidReady_Webhook_Strategy_Template_PopsPineDev.md:129-132,159-165`。
2. `stop=entryPrice-side*entryAtr*slM; limit=entryPrice+side*entryAtr*tpM; strategy.exit("X",from_entry="E",stop=stop,limit=limit)` — 訊號時凍結 ATR；`Gold_Intraday_EMABBVWAP_ATR_SLTP_Reliable_Alerts.md:237-266`。
3. `stop=position_avg_price-side*atr*slM; limit=position_avg_price+side*atr*tpM; strategy.exit("X",from_entry="E",stop=stop,limit=limit)` — 每根 K 以當前 ATR 重送 bracket；`TTAutotune.md:671-674`、`TTLorentzian.md:474-477`、`NNFX_BTC_SSLQQE_SignalForge.md:118-128`、`Sector_Rotation_Momentum_Framework.md:121-136`。
4. `risk=atr*slM; stop=close-side*risk; limit=close+side*risk*rr; strategy.exit("X",from_entry="E",stop=stop,limit=limit)` — ATR 停損、風險倍數止盈；`RK_Gold_Sniper_AI_PRO.md:349-372`、`Supertrend_with_Entry_TP1_TP2_and_TP3_njRv2ENC.md:85-101,136-144`。
5. `stop=entryPrice-side*entryAtr*slM; limit=position_avg_price+side*entryAtr*tpM; strategy.exit("X",from_entry="E",stop=stop,limit=limit)` — 訊號時凍結 ATR，但以均價計 TP；`BEST_Strategy_Template.md:370-420,497-501`。
6. `stop=close-side*atr*stopM; strategy.exit("X",from_entry="E",stop=stop)` — 只有 ATR 停損；`MultiFactor_Adaptive_ZScore_Strategy.md:180,190-203`。
7. `stop=(side==1?fvgLow-atr*buf:fvgHigh+atr*buf); limit=close+side*atr*tpM; strategy.exit("X",from_entry="E",stop=stop,limit=limit)` — FVG 結構停損加 ATR 緩衝；`Fair_Value_Gap_Break_of_Structure.md:118-129`。
8. `stop=(side==1?lower-atr*stopM:upper+atr*stopM); strategy.exit("X",from_entry="E",stop=stop,limit=basis)` — 區間外 ATR 停損、均線止盈；`Mean_Reversion_Range_Trading.md:69-96`。
9. `stop=position_avg_price-side*atr*stopM; strategy.exit("X",from_entry="E",stop=stop,limit=vwap)` — ATR 停損、VWAP 止盈；`2_VWAP_Band_Mean_Reversion.md:69-80`。
10. `stop=useAtr?position_avg_price-side*atr*stopM:orBoundary; risk=side*(position_avg_price-stop); strategy.exit("X",from_entry="E",stop=stop,limit=position_avg_price+side*risk*rr)` — 可切換 ATR／開盤區間停損；`1_Opening_Range_Breakout_ORB.md:86-98`。
11. `strategy.exit("T1",from_entry="E",qty_percent=q1,stop=close-side*atr*slM,limit=close+side*atr*tp1M)` — 同一 stop 配多段 ATR 止盈（T2/T3 換 `q,tpM`）；`Ultimate_Prop_Firm_Artillery.md:151-170`。
12. `if newPosition trail:=close-side*atr*m; trail:=side==1?math.max(trail,close-atr*m):math.min(trail,close+atr*m); strategy.exit("X",from_entry="E",stop=trail)` — 只向有利方向收緊的 close-based ATR trail；`5_Donchian_Intraday_Momentum_Breakout.md:77-86`。
13. `trailHi:=math.max(trailHi,high); if close<trailHi-atr*m strategy.close("Long")` — 高點峰值減 ATR 的市價平倉；`BVOL_STRATEGY.md:76-84`、`Cash_Account_Portfolio_Trend_System_v2.md:211-223,259-277`。
14. `trail:=side==1?math.max(prevTrail,trailSource-atr*m):math.min(prevTrail,trailSource+atr*m); strategy.exit("X",from_entry="E",stop=trail,limit=target)` — 可選 ATR 追蹤／固定 stop；`3Commas_Bot.md:276-293,392,398`。
15. `if tp1Hit rStop:=side==1?math.max(rStop,close-atr*trailM):math.min(rStop,close+atr*trailM); strategy.exit("X",from_entry="E",stop=rStop,limit=target)` — TP1 後才啟用 ATR trail；`Vegas_Channel_Tunnel_Strategy_v11.md:153-221`。
16. `stop:=side==1?math.max(structStop,close-atr*trailM):math.min(structStop,close+atr*trailM); strategy.exit("X",from_entry="E",stop=stop,limit=target)` — 結構停損與 ATR backstop 取較緊者；`Liquidity_Sweep_Reversal.md:533-565`。
17. `stop:=side==1?math.max(position_avg_price,highSinceEntry-activeAtr*wideM):math.min(position_avg_price,lowSinceEntry+activeAtr*wideM); strategy.exit("X",from_entry="E",stop=stop)` — 到利潤門檻後以極值±ATR 寬追蹤，初始結構 stop 另有 ATR 風險上限；`SVT_Big_Swing_Capture_Early_Catch.md:606-678`。
18. `trail:=src>prevTrail?math.max(prevTrail,src-atr*m):math.min(prevTrail,src+atr*m); strategy.exit("X",from_entry="E",stop=trail,limit=target)` — UT 的 ATR 狀態機 trail；`Rabiah6X_UT_Bot_11_Scalp_Targets_DEMA_FVG.md:95-97,160-173`。
19. `trail:=side==1?math.max(prevTrail,ta.lowest(low,n)-atr*m):math.min(prevTrail,ta.highest(high,n)+atr*m); strategy.exit("X",from_entry="E",stop=trail)` — target 後以 lookback 極值±ATR 追蹤；`Bjorgum_Double_Tap.md:372-388,620-636`。
20. `stop=patternAnchor-side*atr*buffer; strategy.exit("X",from_entry="E",stop=stop,limit=measuredMoveTarget)` — 圖形錨點的 ATR 緩衝停損；`Auto_Pattern_Detector_Targets_MarkitTick_Session_Strategy.md:283-292,827-837`。
21. `trailCandidate=side==1?bestClose-entryAtr*trailM:bestClose+entryAtr*trailM; activeStop:=side==1?math.max(activeStop,trailCandidate):math.min(activeStop,trailCandidate); strategy.exit("X",from_entry="E",stop=activeStop,limit=target)` — 用入場 ATR 的單向收盤價追蹤；`ORB_Laboratory_v1012.md:1563-1613,2171-2198`。
22. `newStop=side==1?close-atr*m:close+atr*m; trail:=side==1?math.max(trail,newStop):math.min(trail,newStop); if side*close<side*trail position:=0` — 指標內部持倉狀態的 ATR trail 平倉；`The_Oloid_OWMA_Oloid_Weighted_Moving_Average.md:301,353-391`。
