<!-- tradingview-pine-id: PUB;2e27e8d72bf54ffe90ded2aba2443674 -->
<!-- tradingviewscripts-format: 1 -->
# Volume Forge

Source: https://www.tradingview.com/script/oxzoa1Va-Volume-Forge/

## Description

█ OVERVIEW
Volume Forge is a modular volume flow analysis panel designed to assess the quality and direction of market activity by combining several complementary volume analysis methods.

Most volume tools answer only one question. Delta shows buying and selling pressure. RVOL indicates whether volume is above average. CMF measures capital flow. Weis Wave determines the direction of the dominant wave, while volume efficiency analysis helps detect absorption. Each of these tools is valuable, but when analyzed separately they present only a fragment of the market picture.

Volume Forge was designed as a modular system because a single volume indicator does not allow the market to be evaluated from a broader perspective. High volume does not yet mean buyer dominance. Positive delta does not necessarily mean capital inflow. Strong money flow does not always translate into an efficient price move. Only the mutual agreement of several independent methods makes it possible to determine whether the observed impulse is truly supported by volume.

Each module analyzes a different aspect of the same phenomenon, while the central element of the indicator is Flow Confluence – a weighted scoring system that compares the results of all components and evaluates the degree of their agreement. Instead of manually analyzing several separate indicators, the user receives one coherent panel that shows both the details of individual metrics and their final assessment.

The result is a tool that allows multidimensional volume analysis – from short-term buying and selling pressure, through relative market activity strength and capital flow, to the evaluation of signal agreement and the detection of potential signs of trend exhaustion.

█ CONCEPTS
Delta Oscillator
Delta Oscillator is the main module of the indicator and is responsible for assessing the direction of buying and selling pressure. Instead of using real delta from the futures market, it uses approximate directional volume calculated from the relationship between the candle's open and close, scaled by the candle's total range. The values are then accumulated and smoothed, creating an oscillator that shows the dominance of one side of the market.

Delta primarily answers the question:
Is buying or selling pressure currently dominant?

Relative Volume (RVOL)
RVOL compares current volume with its historical average, showing whether market activity is average, elevated or extreme.

The direction of a price move alone says little without information about the strength of market participation. RVOL allows impulses supported by high activity to be distinguished from moves that occur on low volume.

RVOL answers the question:
Is the current move taking place on sufficiently high volume?

Chaikin Money Flow (CMF)
CMF analyzes capital flow by taking into account both volume and the position of the close within the candle’s range.

Unlike Delta, which measures short-term directional pressure, CMF shows whether capital is actually flowing into or out of the instrument. This allows it to confirm or challenge signals generated by the other modules.

CMF answers the question:
Is money actually flowing into the market?

Volume Efficiency / Climax
High volume does not always lead to a large price move. Sometimes the market generates very high activity while the price remains almost motionless.

The Volume Efficiency module identifies exactly these situations by comparing the size of the price move with volume and filtering them relative to ATR. High volume with low efficiency may indicate absorption or the final phase of an impulse.

This is an informational module that provides additional context but does not affect the final Flow Confluence score.

Weis Wave
Weis Wave presents the direction of the dominant market wave instead of analyzing individual candles.

The module uses ATR-based volatility to determine moments of direction change and displays a subtle background that shows the currently dominant wave.

It answers the question:
What is the broader direction of market movement?

Flow Confluence
Flow Confluence is the most important element of the entire indicator.

It is not another oscillator, but analyzes the agreement of all the remaining modules. Delta, Delta direction, RVOL, CMF level, CMF direction and Weis Wave cast votes for the buying or selling side. Each component has its own weight, so the user can decide which elements should have the greatest importance.

The Flow Confluence table shows both the number of agreeing modules and the final point score. Signals appear only when the obtained score exceeds the defined threshold.

Flow Confluence answers the question:
Do all volume analysis methods point in the same direction?

Divergence Detection
The final layer is the analysis of divergences between price and the Delta oscillator.

If price makes a new extreme while Delta does not confirm the move, this may indicate weakening pressure from one side of the market and increase the probability of a correction or trend reversal.

Divergences serve a warning function and provide additional context when interpreting the other modules.

Why combine multiple volume tools?
Each method analyzes a different aspect of volume flow:

• Delta → Who currently dominates – buyers or sellers?
• RVOL → Is market activity high enough?
• CMF → Is capital actually flowing in or out?
• Volume Efficiency → Does volume translate into price movement, or is it being absorbed?
• Weis Wave → What is the dominant direction of the broader wave?
• Flow Confluence → Do all modules confirm the same scenario?
• Divergences → Is price starting to diverge from volume?

█ FEATURES
Oscillator Settings
• Show Delta Oscillator – displays the main Delta oscillator together with gradient fill. Hiding the oscillator does not disable its calculations – the module continues to participate in the Flow Confluence scoring.
• Cumulative Delta Length – number of bars used to calculate cumulative directional volume.
• Smoothing Length (EMA) – length of the EMA that smooths the Delta oscillator. Higher values produce a smoother but slower-reacting oscillator.
• Delta Area Gradient Transparency – sets the transparency of the fill between the Delta oscillator and the zero level.

RVOL Settings
• Show RVOL Histogram – displays the Relative Volume histogram. Hiding the histogram does not disable calculations – RVOL continues to feed the Climax and Flow Confluence modules.
• RVOL Average Length – number of bars used to calculate the average reference volume.
• Threshold – Weak – level above which volume is considered slightly elevated.
• Threshold – Medium – medium-high volume level. It is also used by the Flow Confluence scoring.
• Threshold – Strong – defines the strong Relative Volume level.
• Threshold – Extreme – defines the exceptionally high volume level used for the strongest histogram highlighting.

CMF Settings
• Show CMF – displays the Chaikin Money Flow wave above the overbought level and below the oversold level.
• CMF Period – number of bars used for Chaikin Money Flow calculations.
• CMF Gradient Transparency – sets the transparency of the CMF wave gradient fill.

Volume Efficiency
• Show Climax / Absorption Markers – displays markers on candles where very high volume did not translate into a correspondingly large price move, which may indicate absorption or culmination of activity.
• Efficiency Normalization Period – number of bars used to normalize volume efficiency.
• RVOL Threshold for Climax / Absorption – minimum Relative Volume required to classify a candle as potential absorption.
• Max Normalized Efficiency – maximum allowed price move efficiency. Lower values result in more selective absorption detection.
• Max Candle Range relative to ATR – prevents large impulsive candles from being marked as absorption by limiting their maximum range relative to ATR.

Weis Wave – Oscillator Background
• Highlight background by Weis Wave direction – colors the pane background according to the direction of the current Weis Wave.
• ATR Length – ATR period used to determine the minimum move required to change wave direction.
• ATR Multiplier – determines how large a price move is required to reverse the Weis Wave direction.
• Highlight Transparency – transparency of the Weis Wave background.
• Highlight Height – height of the highlighted area above the overbought level or below the oversold level.

Divergence Settings
• Enable Divergence Detection – activates detection of divergences between price and the Delta oscillator.
• Pivot Length (Left / Right) – number of bars required to confirm local pivots used when searching for divergences.

Threshold Levels
• Overbought Threshold – defines the Delta oscillator level considered market overbought.
• Oversold Threshold – defines the Delta oscillator level considered market oversold.
• OB/OS Line Width – thickness of the overbought and oversold level lines.
• OB/OS Gradient Transparency – Close to Level – sets line visibility when the oscillator is near the level.
• OB/OS Gradient Transparency – Far From Level – sets line visibility when the oscillator is far from the level.

Flow Confluence
• Show Flow Confluence Table – displays the table summarizing all modules participating in the scoring.
• Table Position – sets the position of the table on the chart.
• Table Text Size – sets the font size.

Flow Confluence – Weights
Each module can receive its own weight that determines its influence on the final point score.
• Weight: Delta Oscillator – points awarded for the oscillator position relative to the zero level.
• Weight: Delta Direction – points awarded for the rise or fall of the Delta oscillator.
• Weight: RVOL – points awarded when Relative Volume exceeds the medium threshold in line with candle direction.
• Weight: CMF Level – points awarded for confirmation of positive or negative capital flow.
• Weight: CMF Direction – points awarded for accelerating or weakening capital flow.
• Weight: Weis Wave – points awarded for agreement with the direction of the current Weis Wave.

Flow Confluence – Signals
• Enable Flow Confluence Signal – displays BUY and SELL diamonds after the score exceeds the defined threshold.
Signals appear while the candle is still forming, but due to price fluctuations they may also disappear during that time. The signal becomes permanent and does not disappear only after the candle closes.
• Minimum score for a signal – minimum number of points required to generate a signal.
• Signal only on first occurrence – prevents multiple signals from being displayed while the same condition remains active.
• Buy Signal Color – color of the bullish diamonds.
• Sell Signal Color – color of the bearish diamonds.

Colors
• Zero Line – color of the Delta oscillator zero level.
• Bullish Color – main color used by all positive elements of the indicator.
• Bearish Color – main color used by all negative elements of the indicator.

█ APPLICATIONS
Assessing the quality of market moves
The primary use of the indicator is to evaluate whether the current price move is truly supported by volume. By analyzing several independent metrics the user receives a much more complete picture than when using a single volume indicator.

Filtering weak impulses
Not every dynamic price move is backed by adequate volume. Volume Forge helps distinguish impulses supported by real market activity from moves that occur with weak volume confirmation.

Detecting volume absorption
The Climax module identifies situations in which very high volume does not translate into a correspondingly large price move. This may indicate order absorption by larger market participants, profit-taking or gradual exhaustion of the impulse.

Confirming trend direction
Simultaneous analysis of Delta, CMF and Weis Wave makes it possible to determine whether the dominant market direction is confirmed by different aspects of volume flow.

Detecting potential reversals
Divergences between price and the Delta oscillator can signal weakening buying or selling pressure even before a clear trend change appears.

Building a custom volume analysis model
Thanks to the ability to individually weight each module, the user can create their own market assessment model. For example, one trader may place greater emphasis on Delta and RVOL, while another may consider CMF and Weis Wave more important elements of their strategy.

█ NOTES
• Hiding a given module affects only its display. Calculations continue to run in the background and remain available to the Flow Confluence system.
• The Climax / Absorption module is purely informational and does not participate in the final scoring.
• Divergences indicate a discrepancy between price and the Delta oscillator and should be treated as a warning signal, not an automatic reversal signal.
• Flow Confluence signals are best used as a confirming element of market analysis together with structure, price action, support and resistance zones, momentum or other technical analysis tools.
• All module weights are fully configurable, allowing the user to adapt the volume assessment model to their own trading style while retaining the same multi-layered volume flow analysis logic.

---

## Source Code

````pine
// This Pine Script® code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © Uncle_the_shooter

//@version=6
indicator("Volume Forge", overlay=false)

// INPUTS
// Oscillator Settings 
showDelta = input.bool(true, "Show Delta Oscillator", group="Oscillator Settings",
     tooltip="Toggles the display of the Delta oscillator (line + area fill). The Delta module answers the question: is there buying/selling pressure? It estimates directional volume from the candle's close relative to its open, scaled by the candle's range. Delta calculations remain active in the background regardless of this toggle and continue to feed the Flow Confluence module.")
cumLen    = input.int(14, "Cumulative Delta Length", group="Oscillator Settings",
     tooltip="Number of bars summed when calculating the cumulative delta.")
smoothLen = input.int(2, "Smoothing Length (EMA)", group="Oscillator Settings",
     tooltip="Length of the EMA smoothing applied to the cumulative delta — a higher value produces a smoother, slower-reacting oscillator.")
deltaAreaGradientTransp = input.int(65, "Delta Area Gradient Transparency", minval=0, maxval=100, group="Oscillator Settings",
     tooltip="Transparency of the fill under the Delta oscillator line at its extreme (0 = fully opaque, 100 = invisible). The fill always fades to zero at an oscillator value of 0, creating a gradient from this value to fully transparent.")

// RVOL Settings 
showRvol   = input.bool(true, "Show RVOL Histogram", group="RVOL Settings",
     tooltip="Toggles the display of the RVOL histogram in the pane. The RVOL module answers the question: is volume large enough? RVOL calculations remain active in the background regardless of this toggle and continue to feed the Climax and Flow Confluence modules.")
rvolLength = input.int(20, "RVOL Average Length", minval=1, group="RVOL Settings",
     tooltip="Number of bars used to calculate the average reference volume.")
rvolT1     = input.float(1.0, "Threshold – Weak", group="RVOL Settings",
     tooltip="RVOL threshold above which volume is considered slightly elevated (lowest intensity level).")
rvolT2     = input.float(1.5, "Threshold – Medium", group="RVOL Settings",
     tooltip="RVOL threshold for medium-level volume. Also used as the required volume strength in the Flow Confluence module.")
rvolT3     = input.float(2.0, "Threshold – Strong", group="RVOL Settings",
     tooltip="RVOL threshold above which volume is considered strong.")
rvolT4     = input.float(3.0, "Threshold – Extreme", group="RVOL Settings",
     tooltip="RVOL threshold above which volume is considered extreme — the highest intensity level for bar highlighting.")

// CMF Settings 
showCmf   = input.bool(true, "Show CMF", group="CMF Settings",
     tooltip="The CMF (Chaikin Money Flow) module answers the question: is money actually flowing? Toggles the drawing of the CMF wave on the oscillator.")
cmfLength = input.int(50, "CMF Period", minval=1, group="CMF Settings",
     tooltip="Number of bars used to calculate Chaikin Money Flow — a longer period smooths the reading, a shorter one reacts faster to changes.")
cmfTransp = input.int(60, "CMF Gradient Transparency", minval=0, maxval=100, group="CMF Settings",
     tooltip="Transparency of the CMF wave fill (0 = fully opaque, 100 = invisible). The 'extreme' zone (above a fixed threshold) is additionally darkened relative to this base value.")

// Volume Efficiency Settings 
showVolEff     = input.bool(true, "Show Climax/Absorption Markers", group="Volume Efficiency",
     tooltip="The Climax module answers the question: is volume efficient or being absorbed? Toggles the drawing of Climax/Absorption markers on the RVOL histogram. The result is informational only and does not affect the Flow Confluence score.")
effPeriod      = input.int(50, "Efficiency Normalization Period", minval=1, group="Volume Efficiency",
     tooltip="Number of bars used to determine the percentile of volume efficiency (price movement per unit of volume).")
climaxMult     = input.float(1.8, "RVOL Threshold for Climax/Absorption", minval=1.0, step=0.1, group="Volume Efficiency",
     tooltip="Minimum RVOL required for a bar to be considered a Climax/Absorption candidate.")
climaxEffMax   = input.float(0.35, "Max Normalized Efficiency for Climax", minval=0.05, maxval=1.0, step=0.05, group="Volume Efficiency",
     tooltip="Maximum normalized efficiency percentile — below this value, volume is considered inefficient (being absorbed by the market).")
smallRangeMult = input.float(0.8, "Max Candle Range / ATR for Climax", minval=0.1, step=0.1, group="Volume Efficiency",
     tooltip="Maximum candle range relative to ATR. Prevents large, genuine price moves from being mistaken for volume absorption.")

// Weis Wave
grpWeis      = "Weis Wave - Oscillator Background"
showWeisBg   = input.bool(true, "Highlight background by Weis wave direction", group=grpWeis,
     tooltip="The Weis Wave module answers the question: what is the broader wave/trend direction? Toggles a subtle background highlight on the oscillator matching the direction of the current wave.")
weisAtrLen   = input.int(14, "ATR Length (wave)", minval=1, group=grpWeis,
     tooltip="Length of the ATR used to determine the minimum price move required to reverse the Weis wave direction.")
weisAtrMult  = input.float(3.0, "ATR Multiplier (wave)", minval=0.1, step=0.1, group=grpWeis,
     tooltip="ATR multiplier — the higher it is, the larger the price move needed to reverse the wave direction (fewer, but more significant, direction changes).")
weisBgTransp = input.int(85, "Highlight Transparency", minval=0, maxval=100, group=grpWeis,
     tooltip="0 = fully opaque, 100 = invisible. Default 85 = very subtle.")
weisBgHeight = input.float(15.0, "Highlight Height above OB / below OS (oscillator units)", minval=1.0, group=grpWeis,
     tooltip="Fixed height of the highlight band measured from the OB boundary (for an upward wave) or OS boundary (for a downward wave), independent of the pane's range.")

// Divergence Settings 
calculateDivergence  = input.bool(true, "Enable Divergence Detection", group="Divergence Settings",
     tooltip="The Divergence module answers the question: is price diverging from the oscillator? Detects situations where price makes a new high/low but the Delta oscillator does not — a possible sign of a weakening trend. Toggles the entire module.")
pivotLength          = input.int(2, "Pivot Length (Left/Right)", minval=1, group="Divergence Settings",
     tooltip="Number of bars on each side required to confirm a local oscillator high/low when searching for divergence.")

// Threshold Levels 
overbought = input.float(25.0, "Overbought Threshold", step=0.1, group="Threshold Levels",
     tooltip="Overbought level of the Delta oscillator — above this value the market is considered strongly overbought on the buying side.")
oversold   = input.float(-25.0, "Oversold Threshold", step=0.1, group="Threshold Levels",
     tooltip="Oversold level of the Delta oscillator — below this value the market is considered strongly oversold on the selling side.")
obosLineWidth = input.int(4, "OB/OS Line Width", minval=1, maxval=10, group="Threshold Levels",
     tooltip="Line thickness of the Overbought/Oversold levels.")
obosGradientMaxTransp = input.int(30, "OB/OS Gradient Transparency — Close to Level", minval=0, maxval=100, group="Threshold Levels",
     tooltip="Transparency of the OB/OS lines when the Delta oscillator is close to or crossing the given level (0 = fully opaque, 100 = invisible). A lower value gives a more pronounced highlight on a strong signal. For the OB/OS lines to have a uniform, constant transparency (no gradient), set this parameter and 'Far From Level' to the same value, e.g. 10 and 10.")
obosGradientMinTransp = input.int(90, "OB/OS Gradient Transparency — Far From Level", minval=0, maxval=100, group="Threshold Levels",
     tooltip="Transparency of the OB/OS lines when the Delta oscillator is far from the given level (0 = fully opaque, 100 = invisible). A higher value makes the line nearly invisible when the signal is weak. For the OB/OS lines to have a uniform, constant transparency (no gradient), set this parameter and 'Close to Level' to the same value, e.g. 10 and 10.")


// Flow Confluence Settings 
showFcTable        = input.bool(true, "Show Flow Confluence Table", group="Flow Confluence",
     tooltip="The Flow Confluence module answers the question: do all the modules agree? Toggles the display of the summary table on the chart.")
fcTablePos          = input.string("Top Right", "Table Position",
     options=["Top Right", "Top Left", "Bottom Right", "Bottom Left"], group="Flow Confluence",
     tooltip="Position of the Flow Confluence table on the chart.")
fcTableSize          = input.string("Small", "Table Text Size",
     options=["Tiny", "Small", "Normal"], group="Flow Confluence",
     tooltip="Text size used in the Flow Confluence table.")

// Flow Confluence - Weights (how many points each condition awards when met) 
groupFcWeights = "Flow Confluence - Weights"
wDeltaOsc = input.float(20.0, "Weight: Delta Osc (level > / < 0)",        minval=0, group=groupFcWeights, tooltip="Points awarded to the bull/bear side when the delta oscillator is above/below zero.")
wDeltaDir = input.float(10.0, "Weight: Delta Direction (rising / falling)",  minval=0, group=groupFcWeights, tooltip="Points awarded to the bull/bear side when the delta oscillator is rising/falling relative to the previous bar.")
wRvol     = input.float(20.0, "Weight: RVOL (volume strength)",             minval=0, group=groupFcWeights, tooltip="Points awarded to the bull/bear side when RVOL >= the 'Medium' threshold in line with the candle direction.")
wCmfLvl   = input.float(10.0, "Weight: CMF Level (> / < ±5)",            minval=0, group=groupFcWeights, tooltip="Points awarded to the bull/bear side when CMF is above +5 / below -5.")
wCmfDir   = input.float(20.0, "Weight: CMF Direction (accelerating/weakening)", minval=0, group=groupFcWeights, tooltip="Points awarded to the bull/bear side when CMF is rising/falling relative to the previous bar.")
wWeis     = input.float(20.0, "Weight: Weis Wave (wave direction)",        minval=0, group=groupFcWeights, tooltip="Points awarded to the bull/bear side when the direction of the current Weis wave (weisDir) matches that side: an upward wave = bull, a downward wave = bear.")

// Flow Confluence - Score Signal 
groupFcSignal      = "Flow Confluence - Score Signal"
useFcSignal        = input.bool(true, "Enable Flow Confluence Signal (diamonds)", group=groupFcSignal, tooltip="Plots a Buy/Sell diamond when the Flow Confluence score (as a % of the maximum possible score) exceeds the threshold below.")
fcSignalThresh     = input.float(100.0, "Minimum score for a signal (raw points)", minval=0, group=groupFcSignal, tooltip="This is a RAW point value (not a percentage), compared directly against the weighted side's score (Score in the table). The maximum possible value equals the sum of all weights above. Increasing the weights automatically raises this ceiling as well — keep that in mind when setting the threshold.")
useFcFirstOnly     = input.bool(true, "Signal only on first occurrence (no duplicates)", group=groupFcSignal, tooltip="When enabled, the Buy/Sell diamond appears ONLY on the first bar where the signal condition (candle color + score threshold) becomes true. Subsequent bars where the same condition remains active are skipped until the condition clears and occurs again. Disable this to get a signal on EVERY bar that meets the threshold.")
colFcBuy           = input.color(color.rgb(6, 162, 47), "Buy Signal Color", group=groupFcSignal, tooltip="Color of the Flow Confluence buy signal marker (diamond).")
colFcSell          = input.color(color.rgb(207, 23, 23), "Sell Signal Color", group=groupFcSignal, tooltip="Color of the Flow Confluence sell signal marker (diamond).")

// Colors 
zeroLineColor = input.color(color.gray, "Zero Line", group="Colors", tooltip="Color of the Delta oscillator's zero line.")
colBullMain   = input.color(color.rgb(6, 162, 47), "Bullish Color", group="Colors", tooltip="Main bullish color used across all modules of the indicator (oscillator, RVOL, CMF, Weis, Flow Confluence).")
colBearMain   = input.color(color.rgb(207, 23, 23), "Bearish Color", group="Colors", tooltip="Main bearish color used across all modules of the indicator (oscillator, RVOL, CMF, Weis, Flow Confluence).")

// CONSTANTS

bandTransparency  = 40
textColor         = color.white

// HELPER FUNCTIONS

// Linear remap of a value from one range to another
remap(val, inMin, inMax, outMin, outMax) =>
    outMin + (val - inMin) * (outMax - outMin) / (inMax - inMin)

// Darkens a color by a given factor (used for CMF extreme zone)
f_darken(col, factor) =>
    color.rgb(color.r(col) * factor, color.g(col) * factor, color.b(col) * factor, color.t(col))

// VOLUME DELTA OSCILLATOR

// Approximate directional volume based on candle position within its range
delta = high != low ? volume * (close - open) / (high - low) : 0.0

// Rolling sum of delta and volume over cumLen bars
var array<float> deltaArray = array.new_float(cumLen, 0.0)
var array<float> volArray   = array.new_float(cumLen, 0.0)
array.shift(deltaArray)
array.shift(volArray)
array.push(deltaArray, delta)
array.push(volArray, volume)

cumDelta     = array.sum(deltaArray)
cumVol       = array.sum(volArray)
cumDeltaPerc = cumVol != 0 ? cumDelta / cumVol * 100 : 0.0

osc = ta.ema(cumDeltaPerc, smoothLen)

// Dynamic transparency for OB level based on distance from current oscillator value
getDynamicOBColor() =>
    rawDist     = overbought - osc
    clampedDist = math.max(0, math.min(rawDist, overbought))
    transp      = math.round(remap(clampedDist, 0, overbought, obosGradientMaxTransp, obosGradientMinTransp))
    color.new(colBearMain, transp)

// Dynamic transparency for OS level based on distance from current oscillator value
getDynamicOSColor() =>
    rawDist     = osc - oversold
    clampedDist = math.max(0, math.min(rawDist, -oversold))
    transp      = math.round(remap(clampedDist, 0, -oversold, obosGradientMaxTransp, obosGradientMinTransp))
    color.new(colBullMain, transp)

// RVOL (RELATIVE VOLUME)

avgVol = ta.sma(volume, rvolLength)
rvol   = volume / avgVol
dir    = close >= open ? 1.0 : -1.0

// RVOL-based transparency in four tiers, applied to the histogram color
transpRvol = rvol >= rvolT4 ? 0 :
             rvol >= rvolT3 ? 15 :
             rvol >= rvolT2 ? 35 :
             rvol >= rvolT1 ? 50 : 65

colRvol = dir >= 0 ? color.new(colBullMain, transpRvol) : color.new(colBearMain, transpRvol)

signedRvol = rvol * dir * 3.0

// CHAIKIN MONEY FLOW (CMF)

rangeHL_cmf = high - low
mfMult      = rangeHL_cmf != 0.0 ? ((close - low) - (high - close)) / rangeHL_cmf : 0.0
mfVol       = mfMult * volume
sumMfVol    = math.sum(mfVol, cmfLength)
sumVol_cmf  = math.sum(volume, cmfLength)
cmf         = sumVol_cmf != 0.0 ? sumMfVol / sumVol_cmf : 0.0
cmfScaled   = cmf * 100.0


// VOLUME EFFICIENCY / CLIMAX DETECTION

bodyMove = math.abs(close - open)
rawEff   = volume > 0 ? bodyMove / volume : 0.0

normEff = ta.percentrank(rawEff, effPeriod) / 100.0

atrRef     = ta.atr(effPeriod)
smallRange = (high - low) < atrRef * smallRangeMult

climaxCond = rvol > climaxMult and normEff < climaxEffMax and smallRange

// WEIS WAVE DIRECTION 

weisWaveSize = ta.atr(weisAtrLen) * weisAtrMult

var int   weisDir     = 0
var float weisExtreme = na

if barstate.isfirst
    weisDir     := close > close[1] ? 1 : -1
    weisExtreme := close
else
    if weisDir == 1
        if close > weisExtreme
            weisExtreme := close
        else if close <= weisExtreme - weisWaveSize
            weisDir     := -1
            weisExtreme := close
    else if weisDir == -1
        if close < weisExtreme
            weisExtreme := close
        else if close >= weisExtreme + weisWaveSize
            weisDir     := 1
            weisExtreme := close

// FLOW CONFLUENCE SCORE

// 1. Delta oscillator direction (smoothed, EMA)
bool fcOscBull = osc > 0
bool fcOscBear = osc < 0

// 1b. Direction of CHANGE in the delta oscillator (rising or falling)
bool fcOscDirBull = osc > osc[1]
bool fcOscDirBear = osc < osc[1]

// 2. RVOL strength aligned with candle direction
bool fcRvolBull = dir >= 0 and rvol >= rvolT2
bool fcRvolBear = dir <  0 and rvol >= rvolT2

// 3. CMF level
bool fcCmfLvlBull = cmfScaled > 5.0
bool fcCmfLvlBear = cmfScaled < -5.0

// 4. CMF direction (whether money flow is accelerating or weakening)
bool fcCmfDirBull = cmfScaled > cmfScaled[1]
bool fcCmfDirBear = cmfScaled < cmfScaled[1]

// 5. Weis wave direction (weisDir) — upward = bull, downward = bear
bool fcWeisBull = weisDir == 1
bool fcWeisBear = weisDir == -1

int fcCountBull = (fcOscBull ? 1 : 0) + (fcOscDirBull ? 1 : 0) + (fcRvolBull ? 1 : 0) + (fcCmfLvlBull ? 1 : 0) + (fcCmfDirBull ? 1 : 0) + (fcWeisBull ? 1 : 0)
int fcCountBear = (fcOscBear ? 1 : 0) + (fcOscDirBear ? 1 : 0) + (fcRvolBear ? 1 : 0) + (fcCmfLvlBear ? 1 : 0) + (fcCmfDirBear ? 1 : 0) + (fcWeisBear ? 1 : 0)

float fcVoteBullW = (fcOscBull ? wDeltaOsc : 0.0) + (fcOscDirBull ? wDeltaDir : 0.0) + (fcRvolBull ? wRvol : 0.0) + (fcCmfLvlBull ? wCmfLvl : 0.0) + (fcCmfDirBull ? wCmfDir : 0.0) + (fcWeisBull ? wWeis : 0.0)
float fcVoteBearW = (fcOscBear ? wDeltaOsc : 0.0) + (fcOscDirBear ? wDeltaDir : 0.0) + (fcRvolBear ? wRvol : 0.0) + (fcCmfLvlBear ? wCmfLvl : 0.0) + (fcCmfDirBear ? wCmfDir : 0.0) + (fcWeisBear ? wWeis : 0.0)

float fcMaxScore = wDeltaOsc + wDeltaDir + wRvol + wCmfLvl + wCmfDir + wWeis
bool  fcHasVotes = fcCountBull > 0 or fcCountBear > 0

bool fcIsBullCandle = close > open
bool fcIsBearCandle = close < open

bool fcSideBull = fcIsBullCandle ? true : fcIsBearCandle ? false : (fcVoteBullW >= fcVoteBearW)

int   fcDispCount    = fcSideBull ? fcCountBull : fcCountBear
float fcDispCountPct = fcDispCount / 6.0 * 100.0
float fcDispScore    = fcSideBull ? fcVoteBullW : fcVoteBearW

color fcTotalColor = fcVoteBullW > fcVoteBearW ? colBullMain :
     fcVoteBullW < fcVoteBearW ? colBearMain : color.gray

bool fcBuySignalCond  = useFcSignal and fcHasVotes and fcIsBullCandle and fcSideBull     and fcDispScore >= fcSignalThresh
bool fcSellSignalCond = useFcSignal and fcHasVotes and fcIsBearCandle and not fcSideBull and fcDispScore >= fcSignalThresh

bool fcBuySignal  = useFcFirstOnly ? (fcBuySignalCond  and not fcBuySignalCond[1])  : fcBuySignalCond
bool fcSellSignal = useFcFirstOnly ? (fcSellSignalCond and not fcSellSignalCond[1]) : fcSellSignalCond

fcPos = switch fcTablePos
    "Top Right"    => position.top_right
    "Top Left"     => position.top_left
    "Bottom Right" => position.bottom_right
    => position.bottom_left

fcSz = switch fcTableSize
    "Tiny"   => size.tiny
    "Normal" => size.normal
    => size.small

f_fcLabel(bool b, bool s) => b ? "▲ Bull" : s ? "▼ Bear" : "— Neutral"
f_fcColor(bool b, bool s) => b ? colBullMain : s ? colBearMain : color.gray

f_fcHistBar(float score, float maxScore) =>
    pct = maxScore > 0 ? math.max(0.0, math.min(100.0, score / maxScore * 100.0)) : 0.0
    blocks = math.max(0, math.min(10, int(math.round(pct / 10))))
    histStr = ""
    for i = 1 to 10
        histStr += i <= blocks ? "█" : "░"
    histStr + " " + str.tostring(math.round(pct, 0)) + "%"

var table fcTable = table.new(fcPos, 2, 10, bgcolor=color.new(color.black, 75), border_color=color.new(color.gray, 50), border_width=1)

if showFcTable and barstate.islast
    headerBg = color.new(color.gray, 15)
    table.cell(fcTable, 0, 0, "Flow Confluence", text_color=color.white, text_size=fcSz, bgcolor=headerBg)
    table.cell(fcTable, 1, 0, "Signal",          text_color=color.white, text_size=fcSz, bgcolor=headerBg)

    table.cell(fcTable, 0, 1, "Delta Osc",  text_color=color.white, text_size=fcSz)
    table.cell(fcTable, 1, 1, f_fcLabel(fcOscBull, fcOscBear), text_color=f_fcColor(fcOscBull, fcOscBear), text_size=fcSz)

    table.cell(fcTable, 0, 2, "Delta Direction", text_color=color.white, text_size=fcSz)
    table.cell(fcTable, 1, 2, f_fcLabel(fcOscDirBull, fcOscDirBear), text_color=f_fcColor(fcOscDirBull, fcOscDirBear), text_size=fcSz)

    table.cell(fcTable, 0, 3, "RVOL",       text_color=color.white, text_size=fcSz)
    table.cell(fcTable, 1, 3, f_fcLabel(fcRvolBull, fcRvolBear), text_color=f_fcColor(fcRvolBull, fcRvolBear), text_size=fcSz)

    table.cell(fcTable, 0, 4, "CMF Level", text_color=color.white, text_size=fcSz)
    table.cell(fcTable, 1, 4, f_fcLabel(fcCmfLvlBull, fcCmfLvlBear), text_color=f_fcColor(fcCmfLvlBull, fcCmfLvlBear), text_size=fcSz)

    table.cell(fcTable, 0, 5, "CMF Direction", text_color=color.white, text_size=fcSz)
    table.cell(fcTable, 1, 5, f_fcLabel(fcCmfDirBull, fcCmfDirBear), text_color=f_fcColor(fcCmfDirBull, fcCmfDirBear), text_size=fcSz)

    table.cell(fcTable, 0, 6, "Weis Wave", text_color=color.white, text_size=fcSz)
    table.cell(fcTable, 1, 6, f_fcLabel(fcWeisBull, fcWeisBear), text_color=f_fcColor(fcWeisBull, fcWeisBear), text_size=fcSz)

    totalBg = color.new(fcTotalColor, 20)
    table.cell(fcTable, 0, 7, "TOTAL", text_color=color.white, text_size=fcSz, bgcolor=totalBg)
    table.cell(fcTable, 1, 7, str.tostring(fcDispCount) + "/6  (" + str.tostring(fcDispCountPct, "#") + "%)",
         text_color=color.white, text_size=fcSz, bgcolor=totalBg)

    color scoreCol = not fcHasVotes ? color.gray : fcSideBull ? colBullMain : colBearMain
    scoreBg = color.new(scoreCol, 20)
    table.cell(fcTable, 0, 8, "Score (pts)", text_color=color.white, text_size=fcSz, bgcolor=scoreBg)
    table.cell(fcTable, 1, 8, str.tostring(math.round(fcDispScore, 0)) + "/" + str.tostring(math.round(fcMaxScore, 0)),
         text_color=color.white, text_size=fcSz, bgcolor=scoreBg)

    table.cell(fcTable, 0, 9, "Score", text_color=color.white, text_size=fcSz, bgcolor=scoreBg)
    table.cell(fcTable, 1, 9, f_fcHistBar(fcDispScore, fcMaxScore), text_color=color.white, text_size=fcSz, bgcolor=scoreBg)


// DIVERGENCE DETECTION

lookbackRight = pivotLength
lookbackLeft  = pivotLength
rangeUpper    = 60
rangeLower    = 5

_inRange(bool cond) =>
    bars = ta.barssince(cond)
    rangeLower <= bars and bars <= rangeUpper

pivotHighOsc = ta.pivothigh(osc, lookbackLeft, lookbackRight)
pivotLowOsc  = ta.pivotlow(osc, lookbackLeft, lookbackRight)

var bool plFound  = false
var bool phFound  = false
var bool bullCond = false
var bool bearCond = false

oscLbr = osc[lookbackRight]

plFound := not na(pivotLowOsc)
phFound := not na(pivotHighOsc)

inRangePl = _inRange(plFound[1])
inRangePh = _inRange(phFound[1])

if calculateDivergence
    oscHl   = oscLbr > ta.valuewhen(plFound, oscLbr, 1) and inRangePl
    lowLbr  = low[lookbackRight]
    priceLl = lowLbr < ta.valuewhen(plFound, lowLbr, 1)
    bullCond := priceLl and oscHl and plFound

    oscLh   = oscLbr < ta.valuewhen(phFound, oscLbr, 1) and inRangePh
    highLbr = high[lookbackRight]
    priceHh = highLbr > ta.valuewhen(phFound, highLbr, 1)
    bearCond := priceHh and oscLh and phFound
else
    bullCond := false
    bearCond := false


// PLOTS — DELTA OSCILLATOR

hline(0, "Zero Line", color=zeroLineColor, linestyle=hline.style_dashed)

plot(overbought, "Overbought Level", color=getDynamicOBColor(), linewidth=obosLineWidth)
plot(oversold,   "Oversold Level",   color=getDynamicOSColor(), linewidth=obosLineWidth)

pOsc  = plot(osc, color=na, display=display.none)
pZero = plot(0,   color=na, display=display.none)

c_bull_peak = color.new(colBullMain, deltaAreaGradientTransp)
c_bear_peak = color.new(colBearMain, deltaAreaGradientTransp)
c_bull_zero = color.new(colBullMain, 100)
c_bear_zero = color.new(colBearMain, 100)

fill(pOsc, pZero,
     top_value    = showDelta and osc > 0 ? osc : na,
     bottom_value = showDelta and osc > 0 ? 0   : na,
     top_color    = showDelta and osc > 0 ? c_bull_peak : na,
     bottom_color = showDelta and osc > 0 ? c_bull_zero : na)

fill(pZero, pOsc,
     top_value    = showDelta and osc < 0 ? 0   : na,
     bottom_value = showDelta and osc < 0 ? osc : na,
     top_color    = showDelta and osc < 0 ? c_bear_zero : na,
     bottom_color = showDelta and osc < 0 ? c_bear_peak : na)

trendUp = osc >= 0
plot(showDelta ? osc : na, "Delta Oscillator", color=trendUp ? colBullMain : colBearMain, linewidth=3)

// PLOTS — CMF WAVE

cmfHeightScale  = 0.5
cmfExtremeLevel = 18.0
cmfExtremeVal   = cmfExtremeLevel * cmfHeightScale

cmfBullVal = cmfScaled > 0 ? cmfScaled * cmfHeightScale : 0.0
cmfBearVal = cmfScaled < 0 ? cmfScaled * cmfHeightScale : 0.0

cmfBullNormal = math.min(cmfBullVal, cmfExtremeVal)
cmfBearNormal = math.max(cmfBearVal, -cmfExtremeVal)

cmfBullTopVal = overbought + cmfBullVal
cmfBearBotVal = oversold   + cmfBearVal

pCmfBase1 = plot(overbought, "CMF Bull Base", color=na, display=display.none)
pCmfBase2 = plot(oversold,   "CMF Bear Base", color=na, display=display.none)

pCmfBullMid = plot(showCmf ? overbought + cmfBullNormal : na, "CMF Bull Mid", color=na, display=display.none)
pCmfBearMid = plot(showCmf ? oversold   + cmfBearNormal : na, "CMF Bear Mid", color=na, display=display.none)

pCmfBullTop = plot(showCmf ? cmfBullTopVal : na, "CMF Bull Top",    color=na, display=display.none)
pCmfBearBot = plot(showCmf ? cmfBearBotVal : na, "CMF Bear Bottom", color=na, display=display.none)

colCmfBullDark = f_darken(colBullMain, 0.55)
colCmfBearDark = f_darken(colBearMain, 0.55)

fill(pCmfBase1, pCmfBullMid, color = showCmf ? color.new(colBullMain, cmfTransp)     : na, title="CMF Bull (normal)")
fill(pCmfBullMid, pCmfBullTop, color = showCmf ? color.new(colCmfBullDark, cmfTransp) : na, title="CMF Bull (extreme)")
fill(pCmfBase2, pCmfBearMid, color = showCmf ? color.new(colBearMain, cmfTransp)     : na, title="CMF Bear (normal)")
fill(pCmfBearMid, pCmfBearBot, color = showCmf ? color.new(colCmfBearDark, cmfTransp) : na, title="CMF Bear (extreme)")


// PLOTS — WEIS WAVE BACKGROUND HIGHLIGHT 

weisLowerBoundVal = oversold    
weisUpperBoundVal = overbought  

weisPaneLow  = weisLowerBoundVal - weisBgHeight
weisPaneHigh = weisUpperBoundVal + weisBgHeight

pWeisPaneLow  = plot(showWeisBg ? weisPaneLow  : na, "Weis BG Pane Low",  color=na, display=display.none)
pWeisPaneHigh = plot(showWeisBg ? weisPaneHigh : na, "Weis BG Pane High", color=na, display=display.none)

pWeisLowerBound = plot(showWeisBg and weisDir == -1 ? weisLowerBoundVal : na, "Weis BG Lower Bound", color=na, display=display.none)
pWeisUpperBound = plot(showWeisBg and weisDir ==  1 ? weisUpperBoundVal : na, "Weis BG Upper Bound", color=na, display=display.none)

fill(pWeisLowerBound, pWeisPaneLow,
     color = showWeisBg and weisDir == -1 ? color.new(colBearMain, weisBgTransp) : na,
     title = "Weis Wave Bearish Background")

fill(pWeisUpperBound, pWeisPaneHigh,
     color = showWeisBg and weisDir == 1 ? color.new(colBullMain, weisBgTransp) : na,
     title = "Weis Wave Bullish Background")

// PLOTS — RVOL HISTOGRAM + CLIMAX MARKERS

plot(showRvol ? signedRvol : na, "RVOL ±", style=plot.style_columns, color=colRvol)

climaxOffset = dir >= 0 ? 1.5 : -1.5
climaxColor  = dir >= 0 ? colBearMain : colBullMain

plotshape(showVolEff and climaxCond ? signedRvol + climaxOffset : na,
     title="Climax / Absorption", style=shape.diamond, location=location.absolute,
     size=size.tiny, color=climaxColor)

// PLOTS — DIVERGENCE

plot(calculateDivergence and plFound ? oscLbr : na, offset=-lookbackRight,
     title="Bullish Divergence", linewidth=2,
     color=bullCond ? colBullMain : color.new(color.white, 100))

plot(calculateDivergence and phFound ? oscLbr : na, offset=-lookbackRight,
     title="Bearish Divergence", linewidth=2,
     color=bearCond ? colBearMain : color.new(color.white, 100))

plotshape(bullCond ? oscLbr : na, offset=-lookbackRight,
     title="Bullish Divergence Label", text=" Bull ", style=shape.labelup,
     location=location.absolute, color=color.new(colBullMain, bandTransparency),
     textcolor=textColor, display=calculateDivergence ? display.pane : display.none)

plotshape(bearCond ? oscLbr : na, offset=-lookbackRight,
     title="Bearish Divergence Label", text=" Bear ", style=shape.labeldown,
     location=location.absolute, color=color.new(colBearMain, bandTransparency),
     textcolor=textColor, display=calculateDivergence ? display.pane : display.none)

// PLOTS — FLOW CONFLUENCE SCORE SIGNAL (Buy/Sell diamonds)

plotshape(fcBuySignal  ? oversold  - 4 : na, title="Flow Confluence Buy Signal",  style=shape.diamond,
     location=location.absolute, size=size.tiny, color=colFcBuy)
plotshape(fcSellSignal ? overbought + 4 : na, title="Flow Confluence Sell Signal", style=shape.diamond,
     location=location.absolute, size=size.tiny, color=colFcSell)

plot(fcBuySignal  ? 1 : na, "backtest_score_buy",  display = display.none)
plot(fcSellSignal ? 1 : na, "backtest_score_sell", display = display.none)

// ALERTS

alertcondition(bullCond, title="Bullish Divergence Detected", message="Bullish divergence detected at pivot.")
alertcondition(bearCond, title="Bearish Divergence Detected", message="Bearish divergence detected at pivot.")
alertcondition(climaxCond, title="Volume Climax / Absorption", message="High volume with low price efficiency — possible absorption/reversal.")

alertcondition(fcBuySignal,  title="Flow Confluence Buy Signal",  message="Flow Confluence score crossed above the signal threshold (bullish).")
alertcondition(fcSellSignal, title="Flow Confluence Sell Signal", message="Flow Confluence score crossed below the signal threshold (bearish).")
````
