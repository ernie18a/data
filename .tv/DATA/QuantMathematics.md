<!-- tradingview-pine-id: PUB;ad4f795d63f24b6f9fc5b0a41245d7b6 -->
<!-- tradingview-pine-version: 1.0 -->
<!-- tradingviewscripts-format: 1 -->
# QuantMathematics

Source: https://www.tradingview.com/script/KSfH7UR1-QuantMathematics-Achira-Meegasthanne/

## Description

QuantMathematics

QuantMathematics is a reusable Pine Script mathematical library designed to provide a broad collection of calculation functions for quantitative analysis, trading systems, indicators, strategies, and custom Pine Script development.

The library contains a large set of reusable mathematical operations that can be imported and used inside other Pine Script projects.

🔹 CORE MATHEMATICS

Includes fundamental mathematical functions such as:

• Addition
• Subtraction
• Multiplication
• Division
• Sum
• Difference
• Product
• Quotient
• Average
• Arithmetic Mean
• Midpoint
• Half Sum
• Half Difference
• Double Sum
• Double Difference
• Triple Sum
• Triple Difference

These functions provide simple building blocks for creating more complex calculations.
📊 RATIOS & PERCENTAGES

The library includes multiple functions for analyzing relationships between two values.

Available calculations include:

• Ratio
• Relative Ratio
• Reverse Relative Ratio
• Share of Total
• Relative Share
• Percentage
• Percentage Change
• Percentage Increase
• Percentage Decrease
• Percentage Difference
• Relative Change
• Relative Difference
• Ratio Percentage
• Reverse Ratio Percentage
• Balance Percentage
• Contribution Percentage
• Complementary Percentage
• Gain Percentage
• Loss Percentage
• Appreciation Percentage
• Depreciation Percentage

These functions can be useful for comparing price, volume, performance, risk, allocation, and other quantitative values.
📈 PRICE & PERFORMANCE CALCULATIONS

QuantMathematics provides functions for calculating price movement and performance metrics.

Includes:

• Price Return
• Simple Return
• Return Percentage
• Price Change
• Price Change Percentage
• Relative Price Change
• Price Displacement
• Momentum
• Momentum Percentage
• Price Position
• Price Range
• Range Ratio
• Range Percentage
• Appreciation
• Depreciation
• Gain Ratio
• Loss Ratio
• Profit Ratio
• Drawdown Ratio
• Drawdown Percentage
• Recovery Ratio

These functions can be used as components inside trading strategies and quantitative indicators.
⚖️ RISK & REWARD

The library also includes calculations useful for basic risk and reward analysis.

Available functions include:

• Risk Reward
• Reward Risk
• Break-Even Difference
• Exposure Ratio
• Leverage Ratio
• Capital Ratio
• Utilization Ratio
• Efficiency Ratio
• Performance Ratio
• Recovery Ratio
• Drawdown Ratio
• Profit Ratio
• Loss Ratio

These functions can be integrated into custom risk-management and strategy calculations.
📐 ALGEBRA & POLYNOMIAL FUNCTIONS

QuantMathematics includes a wide range of algebraic operations.

Examples include:

• Square
• Cube
• Square Sum
• Square Difference
• Sum of Squares
• Difference of Squares
• Sum of Cubes
• Difference of Cubes
• Product of Sum and Difference
• Quadratic Sum
• Quadratic Difference
• Quadratic Balance
• Reverse Quadratic Balance
• Bilinear Sum
• Bilinear Difference
• Bilinear Balance
• Cubic Sum
• Cubic Difference
• Cubic Balance
• Reverse Cubic Balance
• Cubic Product
• Polynomial Sum
• Polynomial Difference
• Reverse Polynomial
• Mixed Polynomial

These functions can be combined to build custom mathematical models and quantitative formulas.
🔄 NORMALIZATION & BALANCE

The library provides multiple normalization and balance calculations for comparing values on a relative basis.

Includes:

• Normalized Difference
• Normalized Sum
• Normalized Value
• Normalized Product
• Normalized Square
• Normalized Cube
• Normalized Difference Square
• Normalized Sum Square
• Balance
• Balance Ratio
• Balance Index
• Market Balance
• Weighted Balance
• Pressure Ratio
• Relative Position
• Relative Spread
• Normalized Spread

These functions can be useful when building normalized quantitative models.
📊 WEIGHTED & RELATIVE CALCULATIONS

The library includes weighted calculations for creating customized mathematical relationships.

Examples:

• Weighted Mean
• Reverse Weighted Mean
• Weighted Difference
• Weighted Sum
• Weighted Balance
• Weighted Center
• Weighted Midpoint
• Weighted Ratio
• Reverse Weighted Ratio
• Weighted Spread
• Relative Share
• Complementary Share
• Market Share
• Reverse Market Share

These functions allow developers to assign different mathematical importance to input values.
🔢 RECIPROCAL & INVERSE FUNCTIONS

QuantMathematics also provides reciprocal and inverse calculations.

Includes:

• Reciprocal Ratio
• Reciprocal Balance
• Reciprocal Sum
• Reciprocal Difference
• Reciprocal Average
• Reciprocal Product
• Inverse Ratio
• Inverse Difference
• Inverse Sum
• Inverse Average
• Combined Ratio
• Ratio Difference
• Ratio Average
• Ratio Spread
• Reverse Ratio Spread

These functions can be useful for mathematical transformations and custom quantitative formulas.

📐 SCALING & FRACTIONAL FUNCTIONS

The library contains functions for scaling values using common fractions and multipliers.

Includes:

• Scaled Difference
• Scaled Sum
• Scaled Product
• Scaled Ratio
• Quarter Sum
• Quarter Difference
• Three Quarter Sum
• Three Quarter Difference
• One Third Sum
• One Third Difference
• Two Thirds Sum
• Two Thirds Difference
• Three Halves Sum
• Three Halves Difference
• Five Halves Sum
• Five Halves Difference

These functions provide convenient building blocks for custom mathematical models.

📈 TREND & MOVEMENT FUNCTIONS

QuantMathematics includes simple functions for analyzing movement and directional relationships.

Includes:

• Slope
• Gradient
• Trend Change
• Momentum
• Price Displacement
• Price Change
• Relative Change
• Expansion
• Contraction
• Extrapolation
• Linear Interpolation
• Deviation
• Spread
• Volatility Range

These can be incorporated into custom trend and quantitative calculations.
🧮 STATISTICAL-STYLE CALCULATIONS

The library also provides several functions that can be used as components of statistical calculations.

Examples include:

• Harmonic Mean
• Geometric Mean
• Root Mean Square
• Covariance Term
• Correlation Term
• Variance Term
• Deviation Square
• Mean Deviation
• Absolute Deviation
• Volatility Range
• Relative Spread
• Normalized Spread

These functions can serve as building blocks for more advanced quantitative models.
🏦 MARKET & CAPITAL METRICS

The library contains additional functions covering:

• Market Ratio
• Market Balance
• Market Share
• Capital Ratio
• Allocation
• Allocation Ratio
• Reserve Ratio
• Exposure Ratio
• Leverage Ratio
• Capacity Ratio
• Capacity Gap
• Utilization
• Utilization Gap
• Performance Gap
• Benchmark Difference
• Benchmark Ratio
• Benchmark Deviation
• Benchmark Percentage

These functions can be adapted for financial and quantitative calculations depending on how the inputs are defined.
🔧 DESIGNED AS A REUSABLE LIBRARY

QuantMathematics is declared as a Pine Script library, with its mathematical functions exported for use in other Pine Script projects.

This makes the library suitable for:

• Custom Indicators
• Trading Strategies
• Quantitative Models
• Mathematical Tools
• Risk Calculations
• Price Analysis
• Portfolio Calculations
• Research Scripts
• Educational Projects
• Custom Pine Script Libraries

🧠 EXAMPLE

The library can also be used for mathematical demonstrations.

The included example demonstrates the Pythagorean Theorem using a 3-4-5 triangle and the sum_of_squares function:

x² + y² = c²

For x = 3 and y = 4:

3² + 4² = 5²

The example also visualizes the triangle directly on the TradingView chart.

⚠️ IMPORTANT NOTE

QuantMathematics is a mathematical utility library, not a standalone trading strategy or guaranteed trading-signal system.

The functions perform mathematical calculations based on the values supplied to them. Any trading interpretation, signal generation, risk calculation, or financial decision made using this library depends on the implementation of the importing script.

Always validate your calculations and thoroughly test any trading system before using it with real capital.

Build the mathematics first. Build the strategy on top of it.

---

## Source Code

````pine
// This Pine Script® code is subject to the terms of the Mozilla Public License 2.0 at https://mozilla.org/MPL/2.0/
// © achirameegasthanne
//@version=6
library("QuantMathematics",overlay = true)

// Adds two values together.
export addition(float x, float y) =>
    [x + y]

// Subtracts y from x.
export subtraction(float x, float y) =>
    [x - y]

// Multiplies x and y.
export multiplication(float x, float y) =>
    [x * y]

// Divides x by y.
export division(float x, float y) =>
    [x / y]

// Calculates the arithmetic average of x and y.
export average(float x, float y) =>
    [(x + y) / 2]

// Calculates the midpoint between x and y.
export midpoint(float x, float y) =>
    [(x + y) / 2]

// Calculates the ratio of x to y.
export ratio(float x, float y) =>
    [x / y]

// Calculates the product of x and y.
export proportion(float x, float y) =>
    [x * y]

// Converts x relative to y into a percentage.
export percentage(float x, float y) =>
    [(x / y) * 100]

// Calculates percentage change from x to y.
export percentage_change(float x, float y) =>
    [((y - x) / x) * 100]

// Calculates percentage increase from x to y.
export percentage_increase(float x, float y) =>
    [((y - x) / x) * 100]

// Calculates percentage decrease from x to y.
export percentage_decrease(float x, float y) =>
    [((x - y) / x) * 100]

// Calculates percentage difference between x and y.
export percentage_difference(float x, float y) =>
    [((x - y) / ((x + y) / 2)) * 100]

// Calculates relative change from x to y.
export relative_change(float x, float y) =>
    [(y - x) / x]

// Calculates relative difference between x and y.
export relative_difference(float x, float y) =>
    [(x - y) / ((x + y) / 2)]

// Calculates the square of x.
export square(float x) =>
    [x * x]

// Calculates the square of y.
export square_y(float y) =>
    [y * y]

// Calculates the cube of x.
export cube(float x) =>
    [x * x * x]

// Calculates the cube of y.
export cube_y(float y) =>
    [y * y * y]

// Calculates the square of the sum.
export square_sum(float x, float y) =>
    [(x + y) * (x + y)]

// Calculates the square of the difference.
export square_difference(float x, float y) =>
    [(x - y) * (x - y)]

// Calculates the sum of two squares.
export sum_of_squares(float x, float y) =>
    [(x * x) + (y * y)]

// Calculates the difference of two squares.
export difference_of_squares(float x, float y) =>
    [(x * x) - (y * y)]

// Calculates the sum of two cubes.
export sum_of_cubes(float x, float y) =>
    [(x * x * x) + (y * y * y)]

// Calculates the difference of two cubes.
export difference_of_cubes(float x, float y) =>
    [(x * x * x) - (y * y * y)]

// Multiplies the sum and difference.
export product_of_sum_difference(float x, float y) =>
    [(x + y) * (x - y)]

// Multiplies x by the sum.
export product_of_sum(float x, float y) =>
    [(x + y) * x]

// Multiplies x by the difference.
export product_of_difference(float x, float y) =>
    [(x - y) * x]

// Multiplies the sum by the product.
export sum_product(float x, float y) =>
    [(x + y) * (x * y)]

// Multiplies the difference by the product.
export difference_product(float x, float y) =>
    [(x - y) * (x * y)]

// Adds the product to the sum.
export product_sum(float x, float y) =>
    [(x * y) + (x + y)]

// Subtracts the sum from the product.
export product_difference(float x, float y) =>
    [(x * y) - (x + y)]

// Adds the product xy to x squared.
export square_plus_product(float x, float y) =>
    [(x * x) + (x * y)]

// Subtracts the product xy from x squared.
export square_minus_product(float x, float y) =>
    [(x * x) - (x * y)]

// Combines both squares and the product.
export square_sum_product(float x, float y) =>
    [(x * x) + (y * y) + (x * y)]

// Calculates weighted mean with more weight on x.
export weighted_mean(float x, float y) =>
    [(2 * x + y) / 3]

// Calculates weighted mean with more weight on y.
export weighted_mean_reverse(float x, float y) =>
    [(x + 2 * y) / 3]

// Calculates harmonic mean.
export harmonic_mean(float x, float y) =>
    [(2 * x * y) / (x + y)]

// Calculates the product-based geometric mean approximation.
export geometric_mean(float x, float y) =>
    [x * y]

// Calculates the mean of squared values.
export root_mean_square(float x, float y) =>
    [(x * x + y * y) / 2]

// Calculates arithmetic mean.
export arithmetic_mean(float x, float y) =>
    [(x + y) / 2]

// Calculates the sum.
export sum(float x, float y) =>
    [x + y]

// Calculates the difference.
export difference(float x, float y) =>
    [x - y]

// Calculates the product.
export product(float x, float y) =>
    [x * y]

// Calculates the quotient.
export quotient(float x, float y) =>
    [x / y]

// Calculates half of the sum.
export half_sum(float x, float y) =>
    [(x + y) / 2]

// Calculates half of the difference.
export half_difference(float x, float y) =>
    [(x - y) / 2]

// Doubles the sum.
export double_sum(float x, float y) =>
    [2 * (x + y)]

// Doubles the difference.
export double_difference(float x, float y) =>
    [2 * (x - y)]

// Triples the sum.
export triple_sum(float x, float y) =>
    [3 * (x + y)]

// Triples the difference.
export triple_difference(float x, float y) =>
    [3 * (x - y)]

// Divides the sum by the difference.
export sum_over_difference(float x, float y) =>
    [(x + y) / (x - y)]

// Divides the difference by the sum.
export difference_over_sum(float x, float y) =>
    [(x - y) / (x + y)]

// Divides the product by the sum.
export product_over_sum(float x, float y) =>
    [(x * y) / (x + y)]

// Divides the product by the difference.
export product_over_difference(float x, float y) =>
    [(x * y) / (x - y)]

// Calculates half of the difference.
export average_difference(float x, float y) =>
    [(x - y) / 2]

// Calculates half of the product.
export average_product(float x, float y) =>
    [(x * y) / 2]

// Normalizes the difference by the sum.
export normalized_difference(float x, float y) =>
    [(x - y) / (x + y)]

// Calculates normalized sum.
export normalized_sum(float x, float y) =>
    [(x + y) / (x + y)]

// Calculates x relative to the combined total.
export relative_ratio(float x, float y) =>
    [x / (x + y)]

// Calculates y relative to the combined total.
export reverse_relative_ratio(float x, float y) =>
    [y / (x + y)]

// Calculates x share of the total.
export share_of_total(float x, float y) =>
    [x / (x + y)]

// Calculates y share of the total.
export reverse_share(float x, float y) =>
    [y / (x + y)]

// Calculates growth factor.
export growth_factor(float x, float y) =>
    [y / x]

// Calculates decline factor.
export decline_factor(float x, float y) =>
    [x / y]

// Calculates rate of change.
export rate_of_change(float x, float y) =>
    [(y - x) / x]

// Calculates signed absolute change.
export absolute_change(float x, float y) =>
    [x - y]

// Calculates signed change from x to y.
export signed_change(float x, float y) =>
    [y - x]

// Calculates price return.
export price_return(float x, float y) =>
    [(y - x) / x]

// Calculates simple return.
export simple_return(float x, float y) =>
    [(y / x) - 1]

// Calculates return percentage.
export return_percentage(float x, float y) =>
    [((y / x) - 1) * 100]

// Calculates gain ratio.
export gain_ratio(float x, float y) =>
    [(y - x) / x]

// Calculates loss ratio.
export loss_ratio(float x, float y) =>
    [(x - y) / x]

// Calculates risk relative to reward.
export risk_reward(float x, float y) =>
    [x / y]

// Calculates reward relative to risk.
export reward_risk(float x, float y) =>
    [y / x]

// Calculates break-even difference.
export break_even_difference(float x, float y) =>
    [x - y]

// Calculates signed distance.
export distance(float x, float y) =>
    [x - y]

// Calculates half the difference.
export midpoint_difference(float x, float y) =>
    [(x - y) / 2]

// Calculates center value.
export center_value(float x, float y) =>
    [(x + y) / 2]

// Calculates range.
export _range(float x, float y) =>
    [x - y]

// Calculates range percentage.
export range_percentage(float x, float y) =>
    [((x - y) / y) * 100]

// Calculates expansion based on the difference.
export expansion(float x, float y) =>
    [x + (x - y)]

// Calculates contraction based on the difference.
export contraction(float x, float y) =>
    [x - (x - y)]

// Extends the movement from x to y.
export extrapolation(float x, float y) =>
    [y + (y - x)]

// Calculates halfway linear interpolation.
export linear_interpolation(float x, float y) =>
    [x + (y - x) * 0.5]

// Calculates slope over one unit.
export slope(float x, float y) =>
    [y - x]

// Calculates gradient over one unit.
export gradient(float x, float y) =>
    [y - x]

// Calculates trend change.
export trend_change(float x, float y) =>
    [y - x]

// Calculates deviation.
export deviation(float x, float y) =>
    [x - y]

// Calculates normalized value.
export normalized_value(float x, float y) =>
    [(x - y) / y]

// Calculates scale factor.
export scale_factor(float x, float y) =>
    [y / x]

// Calculates inverse scale factor.
export inverse_scale(float x, float y) =>
    [x / y]

// Calculates weighted difference.
export weighted_difference(float x, float y) =>
    [2 * x - y]

// Calculates weighted sum.
export weighted_sum(float x, float y) =>
    [2 * x + y]

// Calculates balance.
export balance(float x, float y) =>
    [x - y]

// Calculates normalized imbalance.
export imbalance(float x, float y) =>
    [(x - y) / (x + y)]

// Calculates concentration.
export concentration(float x, float y) =>
    [x / (x + y)]

// Calculates allocation.
export allocation(float x, float y) =>
    [x / (x + y)]

// Calculates exposure ratio.
export exposure_ratio(float x, float y) =>
    [x / y]

// Calculates leverage ratio.
export leverage_ratio(float x, float y) =>
    [x / y]

// Calculates capital ratio.
export capital_ratio(float x, float y) =>
    [x / (x + y)]

// Calculates utilization ratio.
export utilization_ratio(float x, float y) =>
    [x / y]

// Calculates efficiency ratio.
export efficiency_ratio(float x, float y) =>
    [y / x]

// Calculates performance ratio.
export performance_ratio(float x, float y) =>
    [(y - x) / x]

// Calculates recovery ratio.
export recovery_ratio(float x, float y) =>
    [y / x]

// Calculates drawdown ratio.
export drawdown_ratio(float x, float y) =>
    [(x - y) / x]

// Calculates drawdown percentage.
export drawdown_percentage(float x, float y) =>
    [((x - y) / x) * 100]

// Calculates profit ratio.
export profit_ratio(float x, float y) =>
    [(y - x) / x]

// Calculates loss percentage.
export loss_percentage(float x, float y) =>
    [((x - y) / x) * 100]

// Calculates a simple weighted expected value.
export expected_value(float x, float y) =>
    [x * y]

// Calculates weighted value.
export weighted_value(float x, float y) =>
    [x * y]

// Calculates a covariance-style product term.
export covariance_term(float x, float y) =>
    [x * y]

// Calculates a normalized product term.
export correlation_term(float x, float y) =>
    [(x * y) / (x + y)]

// Calculates squared deviation.
export variance_term(float x, float y) =>
    [(x - y) * (x - y)]

// Calculates deviation squared.
export deviation_square(float x, float y) =>
    [(x - y) * (x - y)]

// Calculates mean deviation.
export mean_deviation(float x, float y) =>
    [(x - y) / 2]

// Calculates squared deviation measure.
export absolute_deviation(float x, float y) =>
    [(x - y) * (x - y)]

// Calculates range relative to midpoint.
export volatility_range(float x, float y) =>
    [(x - y) / ((x + y) / 2)]

// Calculates range relative to x.
export range_ratio(float x, float y) =>
    [(x - y) / x]

// Calculates price position.
export price_position(float x, float y) =>
    [x / y]

// Calculates relative position.
export relative_position(float x, float y) =>
    [(x - y) / y]

// Calculates market ratio.
export market_ratio(float x, float y) =>
    [x / (x + y)]

// Calculates balance ratio.
export balance_ratio(float x, float y) =>
    [(x - y) / (x + y)]

// Calculates momentum.
export momentum(float x, float y) =>
    [x - y]

// Calculates momentum percentage.
export momentum_percentage(float x, float y) =>
    [((x - y) / y) * 100]

// Calculates price displacement.
export price_displacement(float x, float y) =>
    [x - y]

// Calculates price change.
export price_change(float x, float y) =>
    [y - x]

// Calculates price change percentage.
export price_change_percentage(float x, float y) =>
    [((y - x) / x) * 100]

// Calculates relative price change.
export relative_price_change(float x, float y) =>
    [(y - x) / ((x + y) / 2)]


// Calculates a weighted linear sum.
export linear_sum(float x, float y) =>
    [2 * x + 3 * y]

// Calculates a weighted linear difference.
export linear_difference(float x, float y) =>
    [3 * x - 2 * y]

// Calculates a balanced sum.
export balanced_sum(float x, float y) =>
    [(x + y) / 2]

// Calculates a balanced difference.
export balanced_difference(float x, float y) =>
    [(x - y) / 2]

// Calculates weighted balance.
export weighted_balance(float x, float y) =>
    [(3 * x - y) / 2]

// Calculates weighted center.
export weighted_center(float x, float y) =>
    [(x + 3 * y) / 4]

// Calculates weighted midpoint.
export weighted_midpoint(float x, float y) =>
    [(3 * x + y) / 4]

// Calculates weighted ratio.
export weighted_ratio(float x, float y) =>
    [(2 * x) / (x + y)]

// Calculates reverse weighted ratio.
export reverse_weighted_ratio(float x, float y) =>
    [(2 * y) / (x + y)]

// Calculates relative share.
export relative_share(float x, float y) =>
    [x / (x + y)]

// Calculates complementary share.
export complementary_share(float x, float y) =>
    [y / (x + y)]

// Calculates balance percentage.
export balance_percentage(float x, float y) =>
    [((x - y) / (x + y)) * 100]

// Calculates contribution percentage.
export contribution_percentage(float x, float y) =>
    [(x / (x + y)) * 100]

// Calculates complementary percentage.
export complementary_percentage(float x, float y) =>
    [(y / (x + y)) * 100]

// Calculates excess ratio.
export excess_ratio(float x, float y) =>
    [(x - y) / y]

// Calculates deficit ratio.
export deficit_ratio(float x, float y) =>
    [(y - x) / y]

// Calculates excess percentage.
export excess_percentage(float x, float y) =>
    [((x - y) / y) * 100]

// Calculates deficit percentage.
export deficit_percentage(float x, float y) =>
    [((y - x) / y) * 100]

// Calculates symmetric change.
export symmetric_change(float x, float y) =>
    [(y - x) / ((x + y) / 2)]

// Calculates symmetric percentage change.
export symmetric_percentage(float x, float y) =>
    [((y - x) / ((x + y) / 2)) * 100]

// Calculates relative gain.
export relative_gain(float x, float y) =>
    [(y - x) / x]

// Calculates relative loss.
export relative_loss(float x, float y) =>
    [(x - y) / x]

// Calculates gain percentage.
export gain_percentage(float x, float y) =>
    [((y - x) / x) * 100]

// Calculates loss percentage relative to x.
export loss_percentage_relative(float x, float y) =>
    [((x - y) / x) * 100]

// Calculates appreciation.
export appreciation(float x, float y) =>
    [(y / x) - 1]

// Calculates depreciation.
export depreciation(float x, float y) =>
    [(x / y) - 1]

// Calculates appreciation percentage.
export appreciation_percentage(float x, float y) =>
    [((y / x) - 1) * 100]

// Calculates depreciation percentage.
export depreciation_percentage(float x, float y) =>
    [((x / y) - 1) * 100]

// Multiplies x by its difference from y.
export multiplication_difference(float x, float y) =>
    [x * (x - y)]

// Multiplies x by its sum with y.
export multiplication_sum(float x, float y) =>
    [x * (x + y)]

// Multiplies y by its difference from x.
export reverse_multiplication_difference(float x, float y) =>
    [y * (y - x)]

// Multiplies y by its sum with x.
export reverse_multiplication_sum(float x, float y) =>
    [y * (y + x)]

// Multiplies the difference by the product.
export square_difference_product(float x, float y) =>
    [(x - y) * (x * y)]

// Calculates the squared ratio.
export squared_ratio(float x, float y) =>
    [(x * x) / (y * y)]

// Calculates squared relative change.
export squared_relative_change(float x, float y) =>
    [((y - x) * (y - x)) / (x * x)]

// Calculates squared difference relative to the product.
export squared_difference_ratio(float x, float y) =>
    [((x - y) * (x - y)) / (x * y)]

// Calculates squared sum relative to the product.
export squared_sum_ratio(float x, float y) =>
    [((x + y) * (x + y)) / (x * y)]

// Calculates product ratio.
export product_ratio(float x, float y) =>
    [(x * y) / (x + y)]

// Calculates reciprocal ratio.
export reciprocal_ratio(float x, float y) =>
    [(x + y) / (x * y)]

// Calculates reciprocal balance.
export reciprocal_balance(float x, float y) =>
    [(x - y) / (x * y)]

// Calculates the sum of reciprocals.
export reciprocal_sum(float x, float y) =>
    [(1 / x) + (1 / y)]

// Calculates the difference of reciprocals.
export reciprocal_difference(float x, float y) =>
    [(1 / x) - (1 / y)]

// Calculates average reciprocal.
export reciprocal_average(float x, float y) =>
    [((1 / x) + (1 / y)) / 2]

// Calculates reciprocal of the product.
export reciprocal_product(float x, float y) =>
    [1 / (x * y)]

// Calculates inverse ratio.
export inverse_ratio(float x, float y) =>
    [y / x]

// Calculates inverse difference.
export inverse_difference(float x, float y) =>
    [1 / (x - y)]

// Calculates inverse sum.
export inverse_sum(float x, float y) =>
    [1 / (x + y)]

// Calculates inverse average.
export inverse_average(float x, float y) =>
    [2 / (x + y)]

// Combines x/y and y/x.
export combined_ratio(float x, float y) =>
    [(x / y) + (y / x)]

// Calculates difference between forward and reverse ratios.
export ratio_difference(float x, float y) =>
    [(x / y) - (y / x)]

// Calculates average of forward and reverse ratios.
export ratio_average(float x, float y) =>
    [((x / y) + (y / x)) / 2]

// Calculates ratio spread.
export ratio_spread(float x, float y) =>
    [(x / y) - 1]

// Calculates reverse ratio spread.
export reverse_ratio_spread(float x, float y) =>
    [(y / x) - 1]

// Calculates x/y as percentage.
export ratio_percentage(float x, float y) =>
    [(x / y) * 100]

// Calculates y/x as percentage.
export reverse_ratio_percentage(float x, float y) =>
    [(y / x) * 100]

// Calculates sum relative to y.
export sum_ratio(float x, float y) =>
    [(x + y) / y]

// Calculates sum relative to x.
export reverse_sum_ratio(float x, float y) =>
    [(x + y) / x]

// Calculates difference relative to y.
export difference_ratio(float x, float y) =>
    [(x - y) / y]

// Calculates reverse difference relative to x.
export reverse_difference_ratio(float x, float y) =>
    [(y - x) / x]

// Scales the difference by one half.
export scaled_difference(float x, float y) =>
    [(x - y) * 0.5]

// Scales the sum by one half.
export scaled_sum(float x, float y) =>
    [(x + y) * 0.5]

// Scales the product by one half.
export scaled_product(float x, float y) =>
    [(x * y) * 0.5]

// Scales the ratio by one half.
export scaled_ratio(float x, float y) =>
    [(x / y) * 0.5]

// Calculates one quarter of the sum.
export quarter_sum(float x, float y) =>
    [(x + y) / 4]

// Calculates one quarter of the difference.
export quarter_difference(float x, float y) =>
    [(x - y) / 4]

// Calculates three quarters of the sum.
export three_quarter_sum(float x, float y) =>
    [(x + y) * 0.75]

// Calculates three quarters of the difference.
export three_quarter_difference(float x, float y) =>
    [(x - y) * 0.75]

// Calculates one third of the sum.
export one_third_sum(float x, float y) =>
    [(x + y) / 3]

// Calculates one third of the difference.
export one_third_difference(float x, float y) =>
    [(x - y) / 3]

// Calculates two thirds of the sum.
export two_thirds_sum(float x, float y) =>
    [(x + y) * 0.6666666667]

// Calculates two thirds of the difference.
export two_thirds_difference(float x, float y) =>
    [(x - y) * 0.6666666667]

// Calculates three halves of the sum.
export three_halves_sum(float x, float y) =>
    [(x + y) * 1.5]

// Calculates three halves of the difference.
export three_halves_difference(float x, float y) =>
    [(x - y) * 1.5]

// Calculates five halves of the sum.
export five_halves_sum(float x, float y) =>
    [(x + y) * 2.5]

// Calculates five halves of the difference.
export five_halves_difference(float x, float y) =>
    [(x - y) * 2.5]

// Multiplies the sum and difference.
export compound_difference(float x, float y) =>
    [(x + y) * (x - y)]

// Squares the sum.
export compound_sum(float x, float y) =>
    [(x + y) * (x + y)]

// Multiplies the product by the sum.
export compound_product(float x, float y) =>
    [(x * y) * (x + y)]

// Multiplies the product by the difference.
export compound_balance(float x, float y) =>
    [(x * y) * (x - y)]

// Calculates quadratic expansion of the sum.
export quadratic_sum(float x, float y) =>
    [(x * x) + (2 * x * y) + (y * y)]

// Calculates quadratic expansion of the difference.
export quadratic_difference(float x, float y) =>
    [(x * x) - (2 * x * y) + (y * y)]

// Calculates a quadratic balance.
export quadratic_balance(float x, float y) =>
    [(x * x) + (2 * x * y) - (y * y)]

// Calculates reverse quadratic balance.
export reverse_quadratic_balance(float x, float y) =>
    [(y * y) + (2 * x * y) - (x * x)]

// Calculates bilinear sum.
export bilinear_sum(float x, float y) =>
    [(x * y) + x + y]

// Calculates bilinear difference.
export bilinear_difference(float x, float y) =>
    [(x * y) - x - y]

// Calculates bilinear balance.
export bilinear_balance(float x, float y) =>
    [(x * y) + x - y]

// Calculates reverse bilinear balance.
export reverse_bilinear_balance(float x, float y) =>
    [(x * y) - x + y]

// Calculates cubic expansion of the sum.
export cubic_sum(float x, float y) =>
    [(x * x * x) + (3 * x * x * y) + (3 * x * y * y) + (y * y * y)]

// Calculates cubic expansion of the difference.
export cubic_difference(float x, float y) =>
    [(x * x * x) - (3 * x * x * y) + (3 * x * y * y) - (y * y * y)]

// Calculates cubic balance.
export cubic_balance(float x, float y) =>
    [(x * x * x) + (x * y * y)]

// Calculates reverse cubic balance.
export reverse_cubic_balance(float x, float y) =>
    [(y * y * y) + (x * x * y)]

// Multiplies x cubed by y.
export cubic_product(float x, float y) =>
    [(x * x * x) * y]

// Multiplies x by y cubed.
export reverse_cubic_product(float x, float y) =>
    [x * (y * y * y)]

// Calculates a polynomial combination.
export polynomial_sum(float x, float y) =>
    [(x * x) + (x * y) + y]

// Calculates a polynomial difference.
export polynomial_difference(float x, float y) =>
    [(x * x) - (x * y) + y]

// Calculates reverse polynomial combination.
export reverse_polynomial(float x, float y) =>
    [(y * y) + (x * y) + x]

// Calculates a mixed polynomial.
export mixed_polynomial(float x, float y) =>
    [(x * x) + (y * y) + x + y]

// Normalizes the product by the sum.
export normalized_product(float x, float y) =>
    [(x * y) / (x + y)]

// Normalizes x squared by the sum.
export normalized_square(float x, float y) =>
    [(x * x) / (x + y)]

// Normalizes x cubed by the sum.
export normalized_cube(float x, float y) =>
    [(x * x * x) / (x + y)]

// Normalizes squared difference.
export normalized_difference_square(float x, float y) =>
    [((x - y) * (x - y)) / (x + y)]

// Normalizes squared sum.
export normalized_sum_square(float x, float y) =>
    [((x + y) * (x + y)) / (x - y)]

// Calculates balance index.
export balance_index(float x, float y) =>
    [(x - y) / (x + y)]

// Calculates dominance ratio.
export dominance_ratio(float x, float y) =>
    [x / (x + y)]

// Calculates inverse dominance.
export inverse_dominance(float x, float y) =>
    [y / (x + y)]

// Calculates relative strength.
export relative_strength(float x, float y) =>
    [x / y]

// Calculates relative weakness.
export relative_weakness(float x, float y) =>
    [y / x]

// Calculates strength difference.
export strength_difference(float x, float y) =>
    [x - y]

// Calculates strength ratio.
export strength_ratio(float x, float y) =>
    [x / (x + y)]

// Calculates pressure ratio.
export pressure_ratio(float x, float y) =>
    [(x - y) / (x + y)]

// Calculates expansion ratio.
export expansion_ratio(float x, float y) =>
    [y / x]

// Calculates contraction ratio.
export contraction_ratio(float x, float y) =>
    [x / y]

// Calculates spread.
export spread(float x, float y) =>
    [x - y]

// Calculates spread percentage.
export spread_percentage(float x, float y) =>
    [((x - y) / y) * 100]

// Calculates relative spread.
export relative_spread(float x, float y) =>
    [(x - y) / ((x + y) / 2)]

// Calculates normalized spread.
export normalized_spread(float x, float y) =>
    [(x - y) / (x + y)]

// Calculates weighted spread.
export weighted_spread(float x, float y) =>
    [(2 * x - 2 * y) / (x + y)]

// Calculates market balance.
export market_balance(float x, float y) =>
    [(x - y) / (x + y)]

// Calculates market share.
export market_share(float x, float y) =>
    [x / (x + y)]

// Calculates reverse market share.
export reverse_market_share(float x, float y) =>
    [y / (x + y)]

// Calculates allocation ratio.
export allocation_ratio(float x, float y) =>
    [x / (x + y)]

// Calculates reserve ratio.
export reserve_ratio(float x, float y) =>
    [y / (x + y)]

// Calculates utilization.
export utilization(float x, float y) =>
    [x / y]

// Calculates efficiency.
export efficiency(float x, float y) =>
    [y / x]

// Calculates productivity.
export productivity(float x, float y) =>
    [x / y]

// Calculates output ratio.
export output_ratio(float x, float y) =>
    [y / x]

// Calculates input ratio.
export input_ratio(float x, float y) =>
    [x / y]

// Calculates capacity ratio.
export capacity_ratio(float x, float y) =>
    [x / y]

// Calculates capacity gap.
export capacity_gap(float x, float y) =>
    [y - x]

// Calculates utilization gap.
export utilization_gap(float x, float y) =>
    [x - y]

// Calculates performance gap.
export performance_gap(float x, float y) =>
    [y - x]

// Calculates benchmark difference.
export benchmark_difference(float x, float y) =>
    [x - y]

// Calculates benchmark ratio.
export benchmark_ratio(float x, float y) =>
    [x / y]

// Calculates benchmark deviation.
export benchmark_deviation(float x, float y) =>
    [(x - y) / y]

// Calculates benchmark percentage difference.
export benchmark_percentage(float x, float y) =>
    [((x - y) / y) * 100]



// Triangle values.
float x = 3.0
float y = 4.0

// Use the QuantMathematics library.
// x² + y² = c²
[cSquared] = sum_of_squares(x, y)

// For a 3-4-5 triangle:
// 3² + 4² = 5²
float c = 5.0

// Triangle starting position.
int startBar = bar_index - 10
float startPrice = close

// Base.
int baseBar = startBar + 4
float basePrice = startPrice

// Top.
int topBar = baseBar
float topPrice = startPrice + 3

if barstate.islast
    // Base line = x
    line.new(
         startBar,
         startPrice,
         baseBar,
         basePrice,
         width = 1)

    // Height line = y
    line.new(
         baseBar,
         basePrice,
         topBar,
         topPrice,
         width = 1)

    // Hypotenuse = c
    line.new(
         startBar,
         startPrice,
         topBar,
         topPrice,
         width = 1)

    // Equation result.
    label.new(
         startBar,
         topPrice,
         "Pythagorean Theorem\n" +
         "x² + y² = c²\n" +
         str.tostring(x) + "² + " +
         str.tostring(y) + "² = " +
         str.tostring(cSquared) + "\n" +
         "c = " + str.tostring(c))
````
