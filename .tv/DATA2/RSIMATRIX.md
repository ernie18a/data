<!-- tradingview-pine-id: PUB;726ea525d14b4c4697803718f3aa0165 -->
<!-- tradingview-pine-version: 2.0 -->
<!-- tradingviewscripts-format: 1 -->
# RSI_MATRIX

Source: https://www.tradingview.com/script/PV6cfnjI/

## Description

RSI MATRIX

INDICATOR CONCEPT
RSI MATRIX was designed to use the RSI in a different way from its traditional representation.
Normally, RSI is displayed in a separate panel to identify momentum, weakness, overbought and oversold conditions.
RSI MATRIX transfers significant RSI events directly onto the price chart, transforming them into horizontal price levels that remain active over time.
The core concept is simple:
A significant RSI event identifies a potentially important price level. That level remains visible until the market confirms its breakout.
This allows the trader to simultaneously observe:
• RSI behavior
• Price levels generated by previous RSI events
• The relationship between current price and active levels
• Confirmed breakouts of those levels
RSI MATRIX is not designed to automatically predict future market direction. It is a market analysis and decision-support tool.

RSI SETTINGS
The indicator uses a fully configurable RSI.
Default configuration:
RSI Period: 14
High Threshold: 65
Low Threshold: 35
Mid Level: 50
These values can be customized in the indicator settings.

RSI SYMBOLS
🔴 ▼ RED DOWN TRIANGLE
The RSI was above the High threshold and crosses back below it.
With the default settings: RSI crosses back below 65.
This event creates a new horizontal ray at the LOW of the candle that generated the signal.
🟢 ▲ GREEN UP TRIANGLE
The RSI was below the Low threshold and crosses back above it.
With the default settings: RSI crosses back above 35.
This event creates a new horizontal ray at the HIGH of the candle that generated the signal.
🟣 ▲ PURPLE UP TRIANGLE
The RSI crosses above the High threshold.
With the default settings: RSI crosses above 65.
This indicates strong positive RSI momentum.
🟣 ▼ PURPLE DOWN TRIANGLE
The RSI crosses below the Low threshold.
With the default settings: RSI crosses below 35.
This indicates strong negative RSI momentum.

RSI 50 LEVEL
RSI MATRIX can optionally display RSI crossings of the central 50 level.
▲ RSI ABOVE 50
The RSI crosses from below to above 50, providing additional information about increasing positive momentum.
▼ RSI BELOW 50
The RSI crosses from above to below 50, providing additional information about increasing negative momentum.
These indications are optional and can be enabled or disabled in the indicator settings.

HORIZONTAL RSI RAYS
Horizontal rays are one of the main features of RSI MATRIX.
When RSI crosses back below the High threshold, the indicator creates a horizontal ray at the LOW of the candle that generated the event.
When RSI crosses back above the Low threshold, the indicator creates a horizontal ray at the HIGH of the candle that generated the event.
The rays extend to the right and remain visible on the chart.
This transforms a past RSI event into a price level that can be monitored in the future.
The trader can therefore observe how price behaves when it later approaches or interacts with these levels.

LINE HIERARCHY
RSI MATRIX includes an automatic visual hierarchy to make active levels easier to read.
Lines closer to the current price can be displayed more prominently, while older lines can gradually become less prominent.
The user can configure:
• Distance from price used to identify nearby lines
• Number of bars after which a line is considered old
• Width of nearby, normal and old lines
• Transparency of each category
This hierarchy only changes the visual appearance of the lines. It does not modify their underlying logic.

CONFIRMED BREAK
A simple intrabar movement beyond a horizontal ray is not sufficient to invalidate it.
RSI MATRIX uses candle closes to confirm a breakout.
When a level is broken according to the confirmation rules:

The BREAK is confirmed
The horizontal ray is deleted
A BREAK symbol can appear on the chart
An alert can be generated
This helps distinguish temporary intrabar penetration from a confirmed breakout.

AUTOMATIC BREAK CONFIRMATION
With Automatic Deletion enabled, RSI MATRIX automatically adapts its confirmation logic according to the timeframe.
M5 AND M15
A BREAK requires 2 closes beyond the line within a 3-bar window. The two closes do not necessarily need to be consecutive.
HIGHER TIMEFRAMES
A BREAK requires 2 consecutive closes beyond the line.
All confirmations are evaluated at candle close.

BREAK DIRECTION
▲ BREAK LONG
A horizontal ray has been broken and confirmed in the upward direction.
▼ BREAK SHORT
A horizontal ray has been broken and confirmed in the downward direction.
LONG and SHORT describe the direction of the confirmed breakout. They should not automatically be interpreted as buy or sell signals.

ALERTS
RSI MATRIX includes automatic alerts for confirmed BREAK events.
Example:
BREAK LONG | TF: M15 | Price: 1.16850
BREAK SHORT | TF: H1 | Price: 1.16520
Each alert contains the BREAK direction, timeframe and price at confirmation.
Alerts are generated at candle close, only after the BREAK confirmation conditions have been satisfied.
This prevents a temporary price movement during candle formation from being treated as a confirmed breakout.

RSI BOX
The current RSI value can be displayed in a box in the upper-right corner of the chart.
▲ RSI xx.x = RSI is greater than or equal to 50
▼ RSI xx.x = RSI is below 50
The box color changes accordingly, providing an immediate view of the current RSI condition without requiring a separate RSI panel.

PRACTICAL USE
RSI MATRIX can be used to create a dynamic map of price levels derived from previous RSI events.
RSI EVENT → LEVEL CREATION → LEVEL REMAINS ACTIVE → PRICE RETURNS → POSSIBLE CONFIRMED BREAK
The objective is not only to observe what RSI is doing now. RSI MATRIX preserves on the price chart the levels associated with previous significant RSI events.
Active horizontal rays represent levels that have not yet been invalidated according to the indicator's confirmation rules.
A confirmed BREAK indicates that price has satisfied the conditions required for that level to be considered broken.

QUICK SYMBOL GUIDE
🔴 ▼ Red Triangle → RSI crosses back below the High threshold → horizontal ray created at the candle LOW.
🟢 ▲ Green Triangle → RSI crosses back above the Low threshold → horizontal ray created at the candle HIGH.
🟣 ▲ Purple Triangle → RSI crosses above the High threshold.
🟣 ▼ Purple Triangle → RSI crosses below the Low threshold.
▲ RSI > 50 → RSI crosses above the central 50 level.
▼ RSI < 50 → RSI crosses below the central 50 level.
━━━━ Horizontal Ray → Price level generated by a previous RSI event and still active.
▲ BREAK LONG → Confirmed upward breakout of an active horizontal ray.
▼ BREAK SHORT → Confirmed downward breakout of an active horizontal ray.

IMPORTANT NOTICE
RSI MATRIX is a technical analysis tool and is not an automated trading system.
Symbols, horizontal rays and BREAK events should be interpreted within the context of market structure and the timeframe being analyzed.
BREAK LONG and BREAK SHORT indicate confirmed breakouts according to the indicator's rules and should not automatically be interpreted as buy or sell signals.

---

## Source Code

````pine
//@version=6
indicator('RSI_MATRIX', overlay = true, max_labels_count = 500, max_lines_count = 300)
// ============================================================================
// TIMEFRAME
// ============================================================================
tf = timeframe.period
f_tf_label(_tf) =>
    string out = _tf
    if _tf == '5'
        out := 'M5'
        out
    else if _tf == '15'
        out := 'M15'
        out
    else if _tf == '30'
        out := 'M30'
        out
    else if _tf == '60'
        out := 'H1'
        out
    else if _tf == '120'
        out := 'H2'
        out
    else if _tf == '240'
        out := 'H4'
        out
    else if _tf == 'D'
        out := 'D1'
        out
    else if _tf == 'W'
        out := 'W1'
        out
    else if _tf == 'M'
        out := 'MN'
        out
    out
    // ============================================================================
    // RSI SETTINGS
    // ============================================================================
showRSI = input.bool(true, 'Show RSI signals', group = 'RSI')
showRSI_50Up = input.bool(false, 'Show RSI above 50%', group = 'RSI')
showRSI_50Dn = input.bool(false, 'Show RSI below 50%', group = 'RSI')
rsiLen = input.int(14, 'RSI Period', minval = 1, group = 'RSI')
rsiHigh = input.int(65, 'RSI High Threshold', minval = 1, maxval = 100, group = 'RSI')
rsiLow = input.int(35, 'RSI Low Threshold', minval = 0, maxval = 99, group = 'RSI')
rsiMid = 50.0
rsi_val = ta.rsi(close, rsiLen)
// ============================================================================
// FIXED RSI BOX - TOP RIGHT
// ============================================================================
groupRSIBOX = 'FIXED RSI BOX'
showRSIBox = input.bool(true, 'Show RSI value', group = groupRSIBOX)
rsiBoxTransp = input.int(15, 'RSI box transparency', minval = 0, maxval = 100, group = groupRSIBOX)
var table rsiBox = table.new(position.top_right, 1, 1, border_width = 1)
if barstate.islast
    table.clear(rsiBox, 0, 0, 0, 0)
    if showRSIBox
        bool rsiBull = rsi_val >= 50
        string arrow = rsiBull ? '▲' : '▼'
        string txt = arrow + ' RSI ' + str.tostring(rsi_val, '#.0')
        color bg = rsiBull ? color.new(color.green, rsiBoxTransp) : color.new(color.red, rsiBoxTransp)
        table.cell(rsiBox, 0, 0, txt, text_color = color.white, bgcolor = bg, text_size = size.normal)
        // ============================================================================
        // RSI SIGNALS
        // ============================================================================
        // Exit from thresholds
exitHigh = ta.crossunder(rsi_val, rsiHigh)
exitLow = ta.crossover(rsi_val, rsiLow)
// Crossing the 50 level
crossUp50 = ta.crossover(rsi_val, rsiMid)
crossDown50 = ta.crossunder(rsi_val, rsiMid)
// Threshold breakouts
crossUpHigh = ta.crossover(rsi_val, rsiHigh)
crossDownLow = ta.crossunder(rsi_val, rsiLow)
// Original colors
colRsiHigh = input.color(color.red, 'RSI High / exit from High color', group = 'RSI - Colors')
colRsiLow = input.color(color.green, 'RSI Low / exit from Low color', group = 'RSI - Colors')
colRsiUp50 = input.color(color.green, 'RSI above 50% color', group = 'RSI - Colors')
colRsiDn50 = input.color(color.red, 'RSI below 50% color', group = 'RSI - Colors')
colRsiUpHigh = input.color(color.purple, 'RSI above High color', group = 'RSI - Colors')
colRsiDnLow = input.color(color.purple, 'RSI below Low color', group = 'RSI - Colors')
// RED triangle: RSI crosses back below High threshold
plotshape(exitHigh, location = location.top, color = colRsiHigh, style = shape.triangledown, title = 'RSI exits High', size = size.tiny, display = showRSI ? display.all : display.none)
// GREEN triangle: RSI crosses back above Low threshold
plotshape(exitLow, location = location.bottom, color = colRsiLow, style = shape.triangleup, title = 'RSI exits Low', size = size.tiny, display = showRSI ? display.all : display.none)
// 50% crossing information
plotshape(crossUp50, location = location.belowbar, color = colRsiUp50, style = shape.labelup, title = 'RSI above 50%', size = size.tiny, display = showRSI and showRSI_50Up ? display.all : display.none)
plotshape(crossDown50, location = location.abovebar, color = colRsiDn50, style = shape.labeldown, title = 'RSI below 50%', size = size.tiny, display = showRSI and showRSI_50Dn ? display.all : display.none)
// PURPLE triangles: threshold breakouts
plotshape(crossUpHigh, location = location.belowbar, color = colRsiUpHigh, style = shape.triangleup, title = 'RSI crosses above High', size = size.tiny, display = showRSI ? display.all : display.none)
plotshape(crossDownLow, location = location.abovebar, color = colRsiDnLow, style = shape.triangledown, title = 'RSI crosses below Low', size = size.tiny, display = showRSI ? display.all : display.none)
// ============================================================================
// LINE STYLE
// ============================================================================
f_line_style(styleStr) =>
    st = line.style_solid
    if styleStr == 'Dotted'
        st := line.style_dotted
        st
    else if styleStr == 'Dashed'
        st := line.style_dashed
        st
    st
groupLINES = 'RSI: HIGH/LOW RAY LINES'
drawLines = input.bool(true, 'Draw High/Low ray lines', group = groupLINES)
lineColor = input.color(color.green, 'Lines / BREAK color', group = groupLINES)
lineWidth = input.int(2, 'Width', minval = 1, maxval = 4, group = groupLINES)
lineTransp = input.int(35, 'Transparency', minval = 0, maxval = 100, group = groupLINES)
lineStyleStr = input.string('Solid', 'Style', options = ['Solid', 'Dotted', 'Dashed'], group = groupLINES)
lineStyle = f_line_style(lineStyleStr)
maxLines = input.int(40, 'Maximum number of lines', minval = 10, maxval = 200, group = groupLINES)
// ============================================================================
// LOCAL LINE RANKING
// ============================================================================
groupRANK = 'LOCAL LINE RANKING'
useLineRanking = input.bool(true, 'Highlight nearby lines / fade old lines', group = groupRANK)
nearPct = input.float(0.15, 'Nearby line within % of price', step = 0.01, minval = 0.01, group = groupRANK)
oldBars = input.int(100, 'Old line after N bars', minval = 10, group = groupRANK)
nearWidth = input.int(3, 'Nearby line width', minval = 1, maxval = 5, group = groupRANK)
normalWidth = input.int(2, 'Normal line width', minval = 1, maxval = 5, group = groupRANK)
oldWidth = input.int(1, 'Old line width', minval = 1, maxval = 5, group = groupRANK)
nearTransp = input.int(10, 'Nearby line transparency', minval = 0, maxval = 100, group = groupRANK)
normalTransp = input.int(45, 'Normal line transparency', minval = 0, maxval = 100, group = groupRANK)
oldTransp = input.int(80, 'Old line transparency', minval = 0, maxval = 100, group = groupRANK)
// ============================================================================
// LINE DELETION - ORIGINAL LOGIC
// ============================================================================
groupAUTO = 'DELETION / TABLE'
useAutoDel = input.bool(true, 'Use automatic deletion', group = groupAUTO)
groupMANUAL = 'MANUAL DELETION'
manualBars = input.int(2, 'Manual: delete after N consecutive closes beyond line', minval = 2, maxval = 20, group = groupMANUAL)
// ============================================================================
// BREAK
// ============================================================================
groupBREAK = 'BREAK: LINE DELETION'
showBreak = input.bool(true, 'Show BREAK when a line is deleted', group = groupBREAK)
breakTransp = input.int(45, 'Transparency BREAK', minval = 0, maxval = 100, group = groupBREAK)
maxBreakLabels = input.int(60, 'Maximum BREAK labels', minval = 10, maxval = 300, group = groupBREAK)
var array<label> breakLabels = array.new_label()
// ============================================================================
// LINE ARRAYS
// ============================================================================
var array<line> highLines = array.new_line()
var array<line> lowLines = array.new_line()
var array<bool> highPending = array.new_bool()
var array<bool> lowPending = array.new_bool()
var array<int> highHits = array.new_int()
var array<int> lowHits = array.new_int()
var array<int> highWindow = array.new_int()
var array<int> lowWindow = array.new_int()
var array<int> highBorn = array.new_int()
var array<int> lowBorn = array.new_int()
// ============================================================================
// LINE CREATION
//
// RSI exits HIGH threshold -> line at candle LOW
// RSI exits LOW threshold -> line at candle HIGH
// ============================================================================
if drawLines and barstate.isconfirmed and exitHigh
    float y = low
    line ln = line.new(bar_index, y, bar_index + 1, y, xloc = xloc.bar_index, extend = extend.right, color = color.new(lineColor, lineTransp), width = lineWidth, style = lineStyle)
    array.push(highLines, ln)
    array.push(highPending, false)
    array.push(highHits, 0)
    array.push(highWindow, 0)
    array.push(highBorn, bar_index)
if drawLines and barstate.isconfirmed and exitLow
    float y = high
    line ln = line.new(bar_index, y, bar_index + 1, y, xloc = xloc.bar_index, extend = extend.right, color = color.new(lineColor, lineTransp), width = lineWidth, style = lineStyle)
    array.push(lowLines, ln)
    array.push(lowPending, false)
    array.push(lowHits, 0)
    array.push(lowWindow, 0)
    array.push(lowBorn, bar_index)
    // ============================================================================
    // DELETION: 2 CONSECUTIVE CLOSES BEYOND THE LINE
    // ============================================================================
f_clean_consecutive(_lines, _pending, _hits, _windows, _born, _breakBelow) =>
    bool breakFound = false
    int sz = array.size(_lines)
    if sz > 0 and barstate.isconfirmed
        for i = sz - 1 to 0 by 1
            line ln = array.get(_lines, i)
            float y = line.get_y1(ln)
            bool previousHit = array.get(_pending, i)
            bool conditionNow = _breakBelow ? close < y : close > y
            bool confirmedBreak = previousHit and conditionNow
            if confirmedBreak
                if showBreak
                    string txt = _breakBelow ? 'BREAK↓' : 'BREAK↑'
                    label lb = label.new(bar_index, _breakBelow ? high : low, txt, xloc = xloc.bar_index, yloc = _breakBelow ? yloc.abovebar : yloc.belowbar, style = _breakBelow ? label.style_label_down : label.style_label_up, color = color.new(lineColor, breakTransp), textcolor = color.white, size = size.tiny)
                    array.push(breakLabels, lb)
                line.delete(ln)
                array.remove(_lines, i)
                array.remove(_pending, i)
                array.remove(_hits, i)
                array.remove(_windows, i)
                array.remove(_born, i)
                breakFound := true
                breakFound
            else
                array.set(_pending, i, conditionNow)
    breakFound
    // ============================================================================
    // M5 / M15 DELETION:
    // 2 CLOSES BEYOND THE LINE WITHIN 3 BARS
    // ============================================================================
f_clean_within3(_lines, _pending, _hits, _windows, _born, _breakBelow) =>
    bool breakFound = false
    int sz = array.size(_lines)
    if sz > 0 and barstate.isconfirmed
        for i = sz - 1 to 0 by 1
            line ln = array.get(_lines, i)
            float y = line.get_y1(ln)
            int hits = array.get(_hits, i)
            int win = array.get(_windows, i)
            bool conditionNow = _breakBelow ? close < y : close > y
            if win > 0
                win := win - 1
                win
            if win <= 0 and hits > 0
                hits := 0
                hits
            if conditionNow
                if hits == 0
                    hits := 1
                    win := 2
                    win
                else
                    hits := hits + 1
                    hits
            bool confirmedBreak = hits >= 2
            if confirmedBreak
                if showBreak
                    string txt = _breakBelow ? 'BREAK↓' : 'BREAK↑'
                    label lb = label.new(bar_index, _breakBelow ? high : low, txt, xloc = xloc.bar_index, yloc = _breakBelow ? yloc.abovebar : yloc.belowbar, style = _breakBelow ? label.style_label_down : label.style_label_up, color = color.new(lineColor, breakTransp), textcolor = color.white, size = size.tiny)
                    array.push(breakLabels, lb)
                line.delete(ln)
                array.remove(_lines, i)
                array.remove(_pending, i)
                array.remove(_hits, i)
                array.remove(_windows, i)
                array.remove(_born, i)
                breakFound := true
                breakFound
            else
                array.set(_hits, i, hits)
                array.set(_windows, i, win)
    breakFound
    // ============================================================================
    // MANUAL DELETION:
    // N CONSECUTIVE CLOSES BEYOND THE LINE
    // ============================================================================
f_clean_afterN(_lines, _pending, _hits, _windows, _born, _breakBelow, _N) =>
    bool breakFound = false
    int sz = array.size(_lines)
    if sz > 0 and barstate.isconfirmed
        for i = sz - 1 to 0 by 1
            line ln = array.get(_lines, i)
            float y = line.get_y1(ln)
            int hits = array.get(_hits, i)
            bool conditionNow = _breakBelow ? close < y : close > y
            if conditionNow
                hits := hits + 1
                hits
            else
                hits := 0
                hits
            bool confirmedBreak = hits >= _N
            if confirmedBreak
                if showBreak
                    string txt = _breakBelow ? 'BREAK↓' : 'BREAK↑'
                    label lb = label.new(bar_index, _breakBelow ? high : low, txt, xloc = xloc.bar_index, yloc = _breakBelow ? yloc.abovebar : yloc.belowbar, style = _breakBelow ? label.style_label_down : label.style_label_up, color = color.new(lineColor, breakTransp), textcolor = color.white, size = size.tiny)
                    array.push(breakLabels, lb)
                line.delete(ln)
                array.remove(_lines, i)
                array.remove(_pending, i)
                array.remove(_hits, i)
                array.remove(_windows, i)
                array.remove(_born, i)
                breakFound := true
                breakFound
            else
                array.set(_hits, i, hits)
    breakFound
    // ============================================================================
    // APPLY BREAK RULES - ORIGINAL LOGIC
    // ============================================================================
bool bearishBreak = false
bool bullishBreak = false
if barstate.isconfirmed
    if useAutoDel
        // AUTOMATIC:
        // M5/M15 = 2 closes beyond the line within 3 bars.
        // Other TFs = 2 consecutive closes beyond the line.
        if tf == '5' or tf == '15'
            bearishBreak := f_clean_within3(highLines, highPending, highHits, highWindow, highBorn, true)
            bullishBreak := f_clean_within3(lowLines, lowPending, lowHits, lowWindow, lowBorn, false)
            bullishBreak
        else
            bearishBreak := f_clean_consecutive(highLines, highPending, highHits, highWindow, highBorn, true)
            bullishBreak := f_clean_consecutive(lowLines, lowPending, lowHits, lowWindow, lowBorn, false)
            bullishBreak
    // MANUAL:
    else // choose in Inputs how many CONSECUTIVE closes must remain beyond the line.
        bearishBreak := f_clean_afterN(highLines, highPending, highHits, highWindow, highBorn, true, manualBars)
        bullishBreak := f_clean_afterN(lowLines, lowPending, lowHits, lowWindow, lowBorn, false, manualBars)
        bullishBreak
        // ============================================================================
        // REFRESH LINE RANKING
        // ============================================================================
f_refresh_ranked(_arr, _born, _baseCol, _baseTransp, _baseWidth, _style) =>
    int sz = array.size(_arr)
    if sz > 0
        for i = 0 to sz - 1 by 1
            line ln = array.get(_arr, i)
            int born = array.get(_born, i)
            float y = line.get_y1(ln)
            int age = bar_index - born
            float distPct = close != 0.0 ? math.abs(close - y) / close * 100.0 : 999.0
            bool isNear = distPct <= nearPct
            bool isOld = age >= oldBars
            int finalTransp = _baseTransp
            int finalWidth = _baseWidth
            if useLineRanking
                finalTransp := isNear ? nearTransp : isOld ? oldTransp : normalTransp
                finalWidth := isNear ? nearWidth : isOld ? oldWidth : normalWidth
                finalWidth
            line.set_color(ln, color.new(_baseCol, finalTransp))
            line.set_width(ln, finalWidth)
            line.set_style(ln, _style)
if barstate.islast
    f_refresh_ranked(highLines, highBorn, lineColor, drawLines ? lineTransp : 100, lineWidth, lineStyle)
    f_refresh_ranked(lowLines, lowBorn, lineColor, drawLines ? lineTransp : 100, lineWidth, lineStyle)
    // ============================================================================
    // OBJECT LIMITS
    // ============================================================================
while array.size(breakLabels) > maxBreakLabels
    label oldLabel = array.shift(breakLabels)
    if not na(oldLabel)
        label.delete(oldLabel)
int totalLines = array.size(highLines) + array.size(lowLines)
while totalLines > maxLines
    if array.size(highLines) >= array.size(lowLines) and array.size(highLines) > 0
        line old = array.shift(highLines)
        array.shift(highPending)
        array.shift(highHits)
        array.shift(highWindow)
        array.shift(highBorn)
        line.delete(old)
    else if array.size(lowLines) > 0
        line old = array.shift(lowLines)
        array.shift(lowPending)
        array.shift(lowHits)
        array.shift(lowWindow)
        array.shift(lowBorn)
        line.delete(old)
    totalLines := array.size(highLines) + array.size(lowLines)
    totalLines
    // ============================================================================
    // BREAK ALERT
    // ============================================================================
if barstate.isconfirmed
    string msg = ''
    if bullishBreak
        msg := 'BREAK LONG | TF: ' + f_tf_label(tf) + ' | Price: ' + str.tostring(close, format.mintick)
        msg
    if bearishBreak
        string shortMsg = 'BREAK SHORT | TF: ' + f_tf_label(tf) + ' | Price: ' + str.tostring(close, format.mintick)
        msg := msg == '' ? shortMsg : msg + ' || ' + shortMsg
        msg
    if msg != ''
        alert(msg, alert.freq_once_per_bar_close)
````
