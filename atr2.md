# ATR exit 補遺（`side=+1` 多、`-1` 空；僅列 `atr.md` 未收錄來源）

1. `st=ta.supertrend(factor,atrLen); stop:=side==1?math.max(nz(stop,st),st):math.min(nz(stop,st),st); target=entryPrice+side*math.abs(entryPrice-st)*rr; strategy.exit("X",from_entry="E",stop=stop,limit=target)` — ATR Supertrend 單向追蹤停損加 R 倍目標；`Legend_BUY_SELL_Supertrend_Filter.md:37-42,165-170,228-245`、`Prakash_BUYSELL_Supertrend_Filter_1_Risk.md:89-92,141-176`。
2. `stop=side==1?math.min(setupFailureLow,setupSupport)-m5ATR*buf:math.max(setupFailureHigh,setupResistance)+m5ATR*buf; limit=entryPrice+side*math.abs(entryPrice-stop)*rr; strategy.exit("X",from_entry="E",stop=stop,limit=limit)` — M5 結構位加 ATR 緩衝、風險倍數止盈；`REVERSAL_CLEAN_MTF_v1_M15_Setup_M5_Entry.md:287,342-402`。
3. `if k1Active trail:=math.max(nz(trail),peakPrice*(1-k1T/100)), exitSig=bar_index>=entryBar+k1W and close<=trail; mfeATR=(peakPrice-entryPrice)/entryATR; if not k1Active and mfeATR>=k1A k1Active:=true; if exitSig strategy.close("Long")` — ATR 只負責啟動，之後以峰值百分比追蹤市價平倉；`BIST30_to_SP_500_ATR_Momentum_Rider.md:239-248,265-266`。
4. `sl=side==1?entry-atr*slM:entry+atr*slM; tp=side==1?entry+atr*tpM:entry-atr*tpM; if (side==1 and (low<=sl or high>=tp)) or (side==-1 and (high>=sl or low<=tp)) arm_close(sl優先於tp)` — 回測器每根 K 以目前 ATR 判定 bracket，雙觸價時停損優先；`TT_Backtest_Compare.md:239-245,230-231`。
5. `inTradeStop=side==1?math.avg(MinOC[1],MinOC[2])-ta.atr(len)*m:math.avg(MaxOC[1],MaxOC[2])+ta.atr(len)*m; if (side==1?Rlow<inTradeStop:Rhigh>inTradeStop) exitOrder=inTradeStop` — 自建回測器的 ATR in-trade stop，下一根以該 stop 模擬出場；`Portfolio_Backtester.md:1311-1313,1371-1372,1437-1441,1480-1489`。
6. `channelAtr=request.security(syminfo.tickerid,higherTF,ta.atr(len)); channelWidth=channelAtr*m; stop=side==1?emaChannel-channelWidth/2:emaChannel+channelWidth/2; strategy.exit("X",from_entry="E",stop=stop,limit=entryPrice+side*math.abs(entryPrice-stop)*rr)` — ATR 寬度 EMA channel 作停損；`Alpha_SR_Channel_Strategy.md:769-772,923-927,956-1031`。
7. `up=src-atrM*ta.atr(atrLen); trend:=trend==1 and close<up[1]?-1:trend; stSell=trend==-1 and trend[1]==1; if stSell and macdSellOk and htfSellOk strategy.close("Long")` — ATR Supertrend 軌道翻空且 MACD／HTF 確認時市價平多；`POLYMR_SupertrendMACD_v1.md:61-76,104-114`。
8. `stop=patternAnchor-side*atr*buffer; strategy.exit("PATTERN-X",from_entry="PATTERN",stop=stop,limit=measuredMoveTarget)` — 與 `atr.md` 第 20 條同型、但前次未列出的重複來源；`Auto_Pattern_Detector_Targets_MarkitTick_Session_Strategy_fgESdFcb.md:280-292,827-837`。
